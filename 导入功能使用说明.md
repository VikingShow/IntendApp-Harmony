# 导入功能使用说明

## 概述

EasyFlow 现已支持从 JSON 和 Excel (CSV) 文件导入项目数据，方便用户在不同平台间迁移项目或批量创建任务。

## 功能特性

### 支持的导入格式

1. **JSON 格式** (.json)
   - 完整保留项目结构
   - 包含任务、依赖关系、检查清单等所有信息
   - 支持画布状态（位置、缩放比例）的恢复
   - 完美支持从导出功能生成的 JSON 文件

2. **CSV 格式** (.csv)
   - 适合批量导入任务列表
   - 支持任务基本信息（名称、日期、坐标等）
   - 支持检查清单进度信息
   - 不包含依赖关系（需要手动添加）

## 使用方法

### 1. 导入 JSON 文件

#### 步骤：

1. 在主页点击右上角的 **"+"** 按钮
2. 选择 **"导入项目"**
3. 在文件选择器中选择 `.json` 文件
4. 系统会自动解析并创建项目
5. 导入成功后自动跳转到项目编辑页面

#### JSON 文件格式示例：

```json
{
  "version": "1.0",
  "exportTime": "2025-01-13T10:30:00.000Z",
  "project": {
    "id": "project_123",
    "name": "我的项目",
    "category": "开发",
    "lastModified": 1705142400000,
    "tasks": [
      {
        "id": "task_1",
        "name": "需求分析",
        "startDate": "2025-01-01T00:00:00.000Z",
        "endDate": "2025-01-05T00:00:00.000Z",
        "x": 100,
        "y": 100,
        "isCritical": true,
        "checklist": [
          {
            "id": "check_1",
            "text": "收集用户需求",
            "isCompleted": true
          }
        ],
        "earlyStart": 0,
        "earlyFinish": 5,
        "lateStart": 0,
        "lateFinish": 5
      }
    ],
    "dependencies": [
      {
        "fromId": "task_1",
        "toId": "task_2"
      }
    ],
    "canvasOffsetX": 0,
    "canvasOffsetY": 0,
    "zoomScale": 1.0
  }
}
```

#### 特点：

- ✅ 完整导入所有项目数据
- ✅ 保留任务间的依赖关系
- ✅ 恢复检查清单和完成状态
- ✅ 恢复画布视图状态
- ✅ 自动生成新的项目 ID（避免冲突）
- ✅ 自动映射任务 ID（确保依赖关系正确）

### 2. 导入 CSV 文件

#### 步骤：

1. 在主页点击右上角的 **"+"** 按钮
2. 选择 **"导入项目"**
3. 在文件选择器中选择 `.csv` 文件
4. 系统会自动解析并创建项目
5. 导入成功后自动跳转到项目编辑页面

#### CSV 文件格式示例：

```csv
任务ID,任务名称,开始日期,结束日期,持续天数,是否关键路径,完成进度,子任务总数,已完成子任务,X坐标,Y坐标
task_1,需求分析,2025/1/1,2025/1/5,5,是,100%,3,3,100,100
task_2,系统设计,2025/1/6,2025/1/10,5,否,50%,2,1,350,100
task_3,编码实现,2025/1/11,2025/1/20,10,是,0%,5,0,100,250
```

#### 注意事项：

- 第一行必须是表头（将被忽略）
- 任务ID 列可以为空（系统会自动生成）
- 日期格式支持：`YYYY/M/D`、`YYYY-M-D` 或 ISO 格式
- 关键路径列：`是`/`否` 或 `true`/`false`
- 坐标为空时，系统会自动排列任务位置

#### 特点：

- ✅ 快速批量导入任务
- ✅ 自动解析任务基本信息
- ✅ 根据子任务数量自动生成检查清单
- ✅ 自动设置任务位置（如果未指定）
- ⚠️ 不包含依赖关系（需要在导入后手动添加）
- ⚠️ 检查清单项为通用名称（需要手动编辑）

## 导入后的操作

### 项目信息调整

导入后，您可能需要：

1. **修改项目名称**
   - 在项目列表中点击项目卡片的 **三点菜单**
   - 选择 **"编辑名称"**
   - 输入新的项目名称

2. **调整任务位置**
   - 进入项目编辑页面
   - 直接拖动任务节点到合适的位置
   - 系统会自动保存新位置

3. **添加依赖关系**（CSV 导入）
   - 在项目编辑页面
   - 长按源任务节点进入连接模式
   - 点击目标任务节点建立依赖关系

4. **编辑检查清单**（CSV 导入）
   - 点击任务节点查看详情
   - 修改检查清单项的文本
   - 调整完成状态

## 数据兼容性

### JSON 格式

- ✅ 与导出功能完全兼容
- ✅ 支持旧版本数据格式（自动升级）
- ✅ 缺失字段会使用默认值填充
- ✅ 无效的依赖关系会自动过滤

### CSV 格式

- ✅ 支持 Excel 导出的 CSV 文件
- ✅ 支持中文日期格式
- ✅ 自动处理引号和逗号转义
- ✅ 跳过统计信息行（导出文件末尾的汇总数据）

## 常见问题

### 1. 导入失败，提示"JSON 格式错误"

**原因**：JSON 文件格式不正确或已损坏

**解决方法**：
- 使用文本编辑器打开 JSON 文件，检查语法是否正确
- 确保文件是从 EasyFlow 导出的（或符合格式规范）
- 尝试使用 JSON 验证工具检查文件有效性

### 2. 导入失败，提示"CSV 文件为空或格式错误"

**原因**：CSV 文件格式不符合要求

**解决方法**：
- 确保文件包含表头行
- 确保至少有一行数据
- 检查是否使用逗号分隔（不是分号或制表符）
- 确保文件编码为 UTF-8

### 3. CSV 导入后任务位置不理想

**原因**：CSV 文件中没有指定坐标，系统使用了默认布局

**解决方法**：
- 在 CSV 文件的 X坐标、Y坐标列中指定具体数值
- 或者导入后手动调整任务位置

### 4. CSV 导入后没有依赖关系

**原因**：CSV 格式本身不支持依赖关系信息

**解决方法**：
- 导入后在编辑页面手动添加依赖关系
- 或者使用 JSON 格式导入（包含完整依赖关系）

### 5. 日期解析错误

**原因**：CSV 文件中的日期格式不被识别

**解决方法**：
- 使用支持的日期格式：`YYYY/M/D`、`YYYY-M-D`
- 确保年、月、日都是数字
- 避免使用中文日期（如 "2025年1月1日"）

### 6. 导入后检查清单项名称不正确

**原因**：CSV 导入只能根据数量生成通用名称

**解决方法**：
- 导入后在任务详情中逐个编辑检查清单项
- 或者使用 JSON 格式导入（包含完整检查清单信息）

## 最佳实践

### 数据迁移

如果需要在不同设备间迁移项目：

1. **推荐使用 JSON 格式**
   - 在源设备上导出项目为 JSON
   - 将 JSON 文件传输到目标设备
   - 在目标设备上导入 JSON 文件
   - 所有数据将完整保留

### 批量创建任务

如果需要快速创建大量任务：

1. **推荐使用 CSV 格式**
   - 在 Excel 或 Google Sheets 中创建任务列表
   - 导出为 CSV 文件
   - 在 EasyFlow 中导入 CSV 文件
   - 导入后补充依赖关系和详细信息

### 数据备份

定期备份重要项目：

1. **导出为 JSON 格式**
   - JSON 文件包含完整信息
   - 可以作为完整备份
   - 便于版本控制和归档

2. **导出为 CSV 格式**
   - CSV 文件便于在 Excel 中查看和编辑
   - 适合制作项目报告
   - 可以导入到其他项目管理工具

## 技术支持

如果在使用导入功能时遇到问题：

1. 检查本文档的"常见问题"部分
2. 查看应用日志（设置 > 开发者选项 > 日志）
3. 联系开发团队获取帮助

## 版本历史

- **v1.0.0** (2025-01-13)
  - ✨ 新增 JSON 格式导入
  - ✨ 新增 CSV 格式导入
  - ✨ 自动检测文件格式
  - ✨ 智能数据验证和转换
  - ✨ 完整的错误处理和用户提示

---

**EasyFlow 开发团队**  
*让项目管理更简单*

