# 缩略模式功能说明

## 概述

缩略模式（Compact Mode）是为手机端优化的项目可视化模式，将任务节点显示为简洁的圆圈，提供更清晰的全局视图和更流畅的触摸交互体验。

## 功能特性

### 1. 自动切换

- **设备检测**：根据屏幕宽度自动检测设备类型
- **手机端**：自动使用缩略模式（`RenderMode.COMPACT`）
- **平板/大屏**：使用普通模式（`RenderMode.NORMAL`）
- **实时更新**：屏幕尺寸变化时自动切换渲染模式

### 2. 视觉设计

#### 节点渲染
- **圆形节点**：24像素半径的圆圈代表任务
- **状态颜色**：
  - 🔴 关键路径：红色（带小红点标记）
  - 🟢 已完成：绿色
  - 🔵 进行中：蓝色
  - ⚪ 未开始：灰色

#### 进度显示
- **进度环**：白色圆环显示任务完成度
- **从12点钟方向**：顺时针绘制进度
- **视觉层次**：进度环在圆圈内侧，不遮挡主体颜色

#### 关键路径标记
- **小红点**：在圆圈右上角显示红色标记
- **突出显示**：快速识别关键任务
- **白色边框**：提高标记的可见性

#### 任务名称
- **位置**：圆圈右侧显示
- **长度限制**：最多8个字符，超出显示省略号
- **字体**：28px HarmonyOS Sans

### 3. 依赖关系渲染

- **连接点**：连线连接到圆圈中心（而不是矩形中心）
- **线条宽度**：
  - 关键路径：2.5像素
  - 普通依赖：1.5像素
- **自动计算**：根据渲染模式自动调整连接点位置

### 4. 交互功能

#### 触摸交互
- **点击节点**：轻触节点（移动距离<5px）显示任务详情对话框
- **拖拽节点**：按住节点并移动可以自由拖拽调整位置
- **拖拽画布**：在空白区域拖拽可以平移画布视图
- **智能识别**：自动区分点击和拖拽操作（基于移动距离阈值）

#### 任务详情对话框
显示内容：
- ✅ 任务名称和关键路径标识
- 📅 时间范围和持续时间
- ⏱️ 时间进度（百分比 + 进度条）
- ✅ 完成进度（百分比 + 进度条）
- 🔗 依赖关系（前置任务 + 后续任务）
- 📋 检查清单（最多显示3项）

操作按钮：
- **编辑任务**：跳转到项目编辑页面
- **删除任务**：引导到编辑页面进行删除

### 5. 画布背景集成

#### 平板/大屏（Home页面）
- 画布作为背景层显示
- 支持触摸交互
- 半透明遮罩效果

#### 手机端Dashboard卡片
- 画布在卡片内显示
- 半透明遮罩层（提高内容可读性）
- 支持触摸交互
- Dashboard内容可滚动

### 6. 响应式设计

- **断点系统**：使用BreakpointSystem管理不同屏幕尺寸
- **自适应布局**：根据设备类型调整界面结构
- **流畅动画**：300ms的切换动画

## 技术实现

### 核心文件

1. **CanvasRenderer.ets**
   - `renderTasksCompact()`：缩略模式节点渲染
   - `renderDependencies()`：依赖线渲染（支持两种模式）
   - `getTaskColor()`：根据状态获取节点颜色

2. **CanvasController.ets**
   - `hitTestCompact()`：圆形区域命中检测
   - `handleTouchDown()`：区分模式的触摸处理
   - `handleTouchUp()`：触发节点点击事件

3. **Home.ets**
   - 画布背景触摸事件处理
   - Dashboard卡片画布交互
   - 任务详情对话框（TaskNodeDetailDialog）
   - 设备检测和模式切换

### 关键算法

#### 圆形命中检测
```typescript
private hitTestCompact(touchX: number, touchY: number, project: Project): TaskNode | null {
  const CIRCLE_RADIUS = 24;
  const HIT_TOLERANCE = 8;
  
  for (let i = project.tasks.length - 1; i >= 0; i--) {
    const task: TaskNode = project.tasks[i];
    const centerX: number = task.x + this.state.canvasOffsetX + 100;
    const centerY: number = task.y + this.state.canvasOffsetY + 60;
    
    const dx = touchX - centerX;
    const dy = touchY - centerY;
    const distance = Math.sqrt(dx * dx + dy * dy);
    
    if (distance <= CIRCLE_RADIUS + HIT_TOLERANCE) {
      return task;
    }
  }
  
  return null;
}
```

#### 点击与拖拽识别
```typescript
private handleTouchUp(): void {
  if (this.state.isDraggingNode && this.state.selectedTask) {
    const finalX: number = this.state.selectedTask.x;
    const finalY: number = this.state.selectedTask.y;
    
    // 计算移动距离（判断是点击还是拖拽）
    const moveDistanceX = Math.abs(finalX - this.state.dragStartX);
    const moveDistanceY = Math.abs(finalY - this.state.dragStartY);
    const CLICK_THRESHOLD = 5; // 移动距离小于5像素认为是点击
    
    const isClick = (moveDistanceX < CLICK_THRESHOLD && moveDistanceY < CLICK_THRESHOLD);
    
    // 缩略模式下，如果是点击，触发节点点击事件
    if (this.state.renderMode === RenderMode.COMPACT && isClick) {
      if (this.onTaskClick) {
        this.onTaskClick(this.state.selectedTask);
      }
    } else if (!isClick) {
      // 如果是拖拽，创建移动命令
      if (this.onTaskMove) {
        this.onTaskMove(this.state.selectedTask.id, finalX, finalY);
      }
    }
  }
}
```

#### 依赖关系查询
```typescript
private getDependencies(): { predecessors: string[], successors: string[] } {
  const predecessors: string[] = [];
  const successors: string[] = [];

  this.project.dependencies.forEach(dep => {
    if (dep.toId === this.task!.id) {
      // 前置任务
      const fromTask = this.project!.tasks.find(t => t.id === dep.fromId);
      if (fromTask) predecessors.push(fromTask.name);
    } else if (dep.fromId === this.task!.id) {
      // 后续任务
      const toTask = this.project!.tasks.find(t => t.id === dep.toId);
      if (toTask) successors.push(toTask.name);
    }
  });

  return { predecessors, successors };
}
```

## 使用场景

1. **手机端项目预览**
   - 快速浏览项目全局结构
   - 查看任务状态和进度
   - 检查依赖关系

2. **触摸交互优化**
   - 避免小屏幕上误触节点
   - 提供更大的点击目标
   - 流畅的画布导航

3. **信息密度优化**
   - 在有限空间内显示更多任务
   - 保持清晰的视觉层次
   - 突出关键信息（关键路径、进度）

## 性能优化

1. **高效渲染**
   - 减少绘制复杂度（圆形比矩形更快）
   - 智能裁剪（只绘制可见区域）
   - 避免重复计算

2. **内存管理**
   - 复用Canvas上下文
   - 及时清理资源
   - 优化触摸事件处理

3. **流畅交互**
   - 300ms动画时长
   - 硬件加速
   - 防抖处理

## 未来增强

可能的功能扩展：
- 🔍 缩放功能（双指捏合）
- 🎨 自定义节点颜色主题
- 📊 节点大小根据优先级调整
- 🔄 手势快捷操作（长按、双击）
- 🎯 快速过滤和搜索
- 🌈 动画效果增强

## 总结

缩略模式为手机端用户提供了优化的项目可视化体验，通过简洁的视觉设计、流畅的触摸交互和完善的信息展示，让用户能够在小屏幕上高效地查看和管理项目。所有核心功能都已实现并可正常使用。

