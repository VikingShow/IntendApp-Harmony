# 可伸缩工具栏手机端优化说明

## 概述

为了提升手机端用户体验，对可伸缩工具栏进行了响应式优化，实现了手机端和平板/PC端的差异化布局。

## 优化内容

### 1. 响应式布局判断

基于 `BreakpointSystem` 的设备类型判断，实现了不同设备的自适应布局：

```typescript
@State isPhone: boolean = false;

// 在 aboutToAppear 中初始化
this.isPhone = BreakpointSystem.isPhone(this.screenWidth);
```

### 2. 工具栏布局优化

#### 手机端（垂直布局）

- **布局方式**：`Column` 垂直排列
- **按钮设计**：
  - 尺寸：36×36 vp（紧凑型）
  - 仅显示图标，不显示文字标签
  - 分隔线：水平线（1×24 vp）
- **展开按钮**：36×36 vp
- **容器样式**：
  - 内边距：`top: 2, bottom: 6, left: 6, right: 6`（顶部紧凑，减少空白）
  - 圆角：24 vp
  - 间距：6 vp
- **定位**：右上角，右对齐

#### 平板/PC端（水平布局）

- **布局方式**：`Row` 水平排列
- **按钮设计**：
  - 高度：32 vp
  - 显示图标 + 文字标签
  - 分隔线：垂直线（1×24 vp）
- **展开按钮**：40×40 vp
- **容器样式**：
  - 内边距：`left: 12, right: 8, top: 6, bottom: 6`
  - 圆角：24 vp
  - 间距：8 vp
- **定位**：右上角，右对齐

### 3. 缩放面板优化

#### 手机端（横向布局）

- **布局方式**：`Row` 横向排列
- **组件顺序**：缩小按钮 → 百分比显示 → 放大按钮 → 分隔线 → 重置按钮
- **按钮设计**：
  - 缩小/放大按钮：28×28 vp
  - 百分比显示：紧凑型文本（11号字体）
  - 重置按钮：紧凑型文本（11号字体）
  - **移除滑块**：由于空间限制，手机端不显示滑块
- **容器样式**：
  - 高度：36 vp
  - 内边距：`left: 10, right: 10, top: 4, bottom: 4`
  - 圆角：20 vp
  - 间距：6 vp
- **定位**：
  - X轴：左对齐（`position: { x: 0, y: 0 }`）
  - 偏移：`translate: { x: 12, y: 12 }`
  - 从**左侧**打开，显示在工具栏**上方**，避免遮挡
  - 动画效果：从左侧滑入（`translate: { x: -30, y: -20 }`）
  - zIndex: 98（低于工具栏的 99）

#### 平板/PC端（垂直布局）

- **布局方式**：`Column` 垂直排列
- **组件顺序**：放大按钮 → 滑块 → 百分比显示 → 缩小按钮 → 分隔线 → 重置按钮
- **滑块设计**：
  - 高度：120 vp
  - 宽度：16 vp
  - 支持拖拽和点击交互
- **容器样式**：
  - 宽度：48 vp
  - 内边距：`top: 10, bottom: 10`
  - 圆角：24 vp
  - 间距：6 vp
- **定位**：
  - X轴：右对齐（`position: { x: '100%', y: 68 }`）
  - 偏移：`translate: { x: -60, y: 0 }`
  - 显示在工具栏**下方**
  - zIndex: 99

### 4. 按钮功能保持一致

无论哪种布局，所有按钮功能保持不变：

- **撤销/重做**：实时响应 `canUndo` 和 `canRedo` 状态
- **锁定/解锁**：实时响应 `canvasLock` 状态
- **重置**：重置画布视图
- **缩略/完整模式**：实时响应 `canvasRenderMode` 状态
- **缩放控制**：实时响应 `zoomScale` 状态

### 5. 动画效果

- **展开/收起动画**：180ms，`Curve.EaseInOut`
- **过渡动画**：两个布局分支都添加了 `.transition()` 修饰符
  - 缩放：0.8
  - 透明度：0 → 1
  - 平移：y: -20 → 0

## 技术要点

### 1. 条件渲染

使用 `if-else` 实现不同设备的差异化布局：

```typescript
if (this.isPhone) {
  // 手机端布局
  Column() { ... }
} else {
  // 平板/PC端布局
  Row() { ... }
}
```

### 2. 修饰符分支处理

由于 ArkTS 不支持在 `if-else` 块后直接添加修饰符，需要分别为每个分支的组件添加：

```typescript
// ❌ 错误写法
if (condition) {
  Component1() { ... }
} else {
  Component2() { ... }
}
.modifier()  // 编译错误！

// ✅ 正确写法
if (condition) {
  Component1() { ... }
  .modifier()
} else {
  Component2() { ... }
  .modifier()
}
```

### 3. 响应式状态绑定

所有按钮直接绑定 `@State` 变量，确保状态变化时 UI 实时更新：

```typescript
.fontColor(this.canUndo ? ColorResource.errorColor : ColorResource.surfaceStrong)
.backgroundColor(this.canUndo ? ColorResource.errorLight : ColorResource.surfaceLight)
.enabled(this.canUndo)
.opacity(this.canUndo ? 1 : 0.5)
```

## 手机端优化效果

### 空间利用

- **工具栏垂直展开**：节省水平空间，不会超出屏幕
- **按钮紧凑设计**：36×36 vp，去除文字标签，仅保留图标
- **缩放面板横向**：紧凑的横向布局，宽度适中，不会超出屏幕

### 位置安排

- **缩放面板在上方**：避免与工具栏重叠
- **右上角对齐**：统一的视觉风格，便于操作

### 交互优化

- **手机端移除滑块**：由于屏幕空间有限，手机端的缩放面板仅提供 +/- 按钮和重置按钮，去除了滑块
- **保留所有功能**：虽然布局紧凑，但所有功能按钮都保留

## 兼容性

- **自动适配**：基于 `BreakpointSystem` 自动判断设备类型
- **断点定义**：
  - 手机：0-599 vp
  - 平板竖屏：600-839 vp
  - 平板横屏：840-1439 vp
  - 折叠屏/2-in-1：1440 vp+
- **动态更新**：窗口尺寸变化时自动切换布局

## 总结

此次优化实现了工具栏的真正响应式设计：

1. **手机端**：
   - 垂直工具栏，顶部紧凑（`top: 2`），减少空白
   - 横向缩放面板从**左侧**打开，避免与工具栏右侧重叠
   - 充分利用有限的屏幕空间
2. **平板/PC端**：横向工具栏 + 垂直缩放面板，提供更丰富的交互（包括滑块）
3. **统一体验**：无论哪种设备，所有功能保持一致，状态实时同步
4. **流畅动画**：
   - 工具栏：180ms 展开/收起动画
   - 缩放面板：从左侧滑入效果（`translate: { x: -30, y: -20 }`）

---

**修改文件**：
- `entry/src/main/ets/pages/Index.ets`

**修改时间**：2025-10-13

