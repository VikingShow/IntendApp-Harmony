# 拖拽功能使用说明

## ✨ 新功能概览

节点工作台现已支持完整的拖拽操作，让您更直观地设计节点布局。

## 🎯 拖拽功能详解

### 1. 从组件库拖拽到画布

**操作步骤：**
1. 在左侧组件库中找到想要的组件
2. **按住组件不放**，开始拖拽
3. 拖拽到画布的**节点卡片预览区域**
4. 看到**蓝色指示线**时松开鼠标
5. 组件会自动添加到指定位置

**拖拽目标区域：**
- **组件之间**：在两个组件中间松开，插入到该位置
- **组件底部**：拖到最后一个组件下方的虚线框，添加到末尾
- **空白画布**：如果还没有组件，拖到中央虚线框即可

**视觉提示：**
- 🔵 **蓝色指示线**：显示组件将插入的位置
- 📦 **虚线框**：拖拽到此处可添加组件
- 🎨 **背景高亮**：拖拽经过时区域会高亮显示

### 2. 画布内组件排序

**操作步骤：**
1. 在画布中找到要移动的组件
2. **按住组件不放**，开始拖拽
3. 拖到目标位置（其他组件上方或下方）
4. 看到**蓝色指示线**时松开
5. 组件顺序自动调整

**排序规则：**
- 向上拖：插入到目标组件**之前**
- 向下拖：插入到目标组件**之后**
- 拖到最底部：移动到列表**末尾**

**智能识别：**
- ✅ 自动计算正确的插入位置
- ✅ 实时显示拖拽反馈
- ✅ 拖拽结束后自动保存

### 3. 点击快速添加（保留）

如果不习惯拖拽，仍可使用点击方式：
- 点击组件库中的组件 → 自动添加到画布底部
- 或点击画布空白区的 `+ 添加标题组件` 按钮

## 📥 导入功能优化

### 两种导入方式

#### 方式一：直接粘贴（推荐）
1. 点击顶部 `导入JSON` 按钮
2. 保持在 **"直接粘贴"** Tab
3. 粘贴JSON内容到文本框
4. 点击 `导入` 按钮

**使用场景：**
- ✅ 从其他地方复制的JSON
- ✅ 从聊天工具收到的配置
- ✅ 快速导入测试数据

#### 方式二：从文件选择（开发中）
1. 点击顶部 `导入JSON` 按钮
2. 切换到 **"从文件选择"** Tab
3. 点击 `选择文件` 按钮
4. 选择 `.json` 文件

**当前状态：**
- ⚠️ 文件选择功能正在开发中
- 💡 暂时请使用"直接粘贴"方式
- 📋 可以先打开文件复制内容，再粘贴导入

### 导入验证机制

系统会自动验证导入的JSON：
- ✅ 检查JSON格式是否正确
- ✅ 验证必要字段（id, name, style, layout）
- ✅ 提示缺失的字段
- ✅ 显示详细的错误信息

**导入成功后：**
- 节点类型会出现在左侧列表
- 自动选中新导入的节点
- 画布实时显示节点效果

## 🎨 拖拽交互细节

### 拖拽时的视觉反馈

**组件库组件：**
- 拖拽时背景色变浅
- 显示阴影效果
- 鼠标变为抓取状态

**画布组件：**
- 拖拽的组件轻微透明
- 目标位置显示蓝色指示线
- 其他组件正常显示

**拖放区域：**
- 进入时背景高亮
- 显示虚线边框
- 离开时恢复正常

### 拖拽的最佳实践

#### ✅ 推荐做法
- **稳定拖拽**：按住不放，平稳移动
- **精准定位**：看到蓝线再松开
- **小步调整**：一次移动一个位置
- **及时保存**：系统会自动保存，无需担心

#### ❌ 避免操作
- **快速甩动**：可能导致位置不准确
- **多次拖拽同一组件**：建议规划好再拖
- **在拖拽中点击**：应该先完成当前拖拽

## 🔧 常见问题

**Q: 拖拽没有反应？**
A: 请确保：
- 按住组件1-2秒再开始拖动
- 拖拽到画布的节点卡片区域内
- 已选择了一个节点类型

**Q: 蓝色指示线不显示？**
A: 这可能是因为：
- 拖拽速度过快，系统未检测到
- 拖到了无效区域（非组件区域）
- 尝试慢一点拖拽

**Q: 组件顺序不对？**
A: 系统会自动计算正确位置：
- 向上拖：插入到目标之前
- 向下拖：插入到目标之后
- 如果顺序不对，可以再次拖拽调整

**Q: 导入JSON失败？**
A: 检查以下内容：
- JSON格式是否正确（使用JSON验证器）
- 是否包含必要字段
- 复制时是否完整（没有截断）

**Q: 拖拽和点击有什么区别？**
A: 
- **拖拽**：可以精确控制插入位置
- **点击**：总是添加到末尾
- 两种方式都可以使用，选择你喜欢的

## 🚀 高级技巧

### 快速布局组件

**场景1：从头设计**
1. 拖入 `TITLE` 到顶部
2. 拖入 `DATE_RANGE` 到第二位
3. 依次添加进度条和其他组件

**场景2：调整现有布局**
1. 选中要移动的组件
2. 拖到目标位置
3. 系统自动重新排序

**场景3：批量添加**
1. 先点击添加多个组件
2. 再用拖拽调整顺序
3. 比逐个拖拽更快

### 导入导出工作流

**分享配置：**
```
1. 设计好节点布局
2. 点击"导出JSON"
3. 复制JSON内容
4. 分享给团队成员
5. 对方点击"导入JSON"粘贴即可
```

**备份配置：**
```
1. 定期点击"导出JSON"
2. 保存到本地文件
3. 需要时随时导入恢复
```

## 📊 性能优化

系统已针对拖拽操作进行优化：
- ⚡ 流畅的拖拽动画
- 💾 自动保存，无需手动
- 🎯 精准的位置计算
- 🔄 实时的视觉反馈

## 🆕 未来计划

- ⏳ 文件选择器集成
- ⏳ 批量导入多个节点
- ⏳ 拖拽时显示组件预览
- ⏳ 撤销/重做拖拽操作
- ⏳ 拖拽手势优化

---

**提示：** 所有拖拽操作都会自动保存，无需手动保存。尽情尝试吧！
