# 布局管理功能说明

## ✨ 新增功能

节点工作台现已支持完整的布局管理功能，让您可以自由控制工作区的显示方式。

## 🎯 功能详解

### 1. 边框颜色和圆角编辑

**位置：** 左侧节点类型列表顶部（选中节点时显示）

**功能：**
- ✅ 边框颜色输入框：输入颜色值（如 `#FF0000`）
- ✅ 留空使用主题色
- ✅ 圆角大小滑块：0-32px，步长2px
- ✅ 实时预览效果
- ✅ 自动保存修改

**使用方法：**
1. 在左侧列表选中一个节点类型
2. 顶部会出现"当前节点样式"编辑区
3. 输入边框颜色或拖动滑块调整圆角
4. 画布中的预览会实时更新

**颜色格式：**
- ✅ 十六进制：`#FF0000`、`#F00`
- ✅ RGB：`rgb(255, 0, 0)`
- ✅ RGBA：`rgba(255, 0, 0, 0.5)`
- ⚠️ 留空则使用主题色作为边框色

### 2. 四栏折叠功能

**折叠按钮位置：** 每个面板右上角/左上角

**面板列表：**
1. **节点类型库**（最左侧）- 右上角 `◀` 按钮
2. **组件库**（中左）- 右上角 `◀` 按钮
3. **可视化画布**（中右）- 右上角 `◀` 按钮
4. **属性面板**（最右侧）- 左上角 `▶` 按钮

**折叠效果：**
- 折叠后宽度变为 **48px**
- 显示竖向文字标识
- 其他面板自动扩展占据空间
- 再次点击按钮可展开

**使用场景：**
- 📋 只看画布：折叠组件库和属性面板
- 🎨 专注设计：折叠节点列表，更大画布
- 📝 编辑属性：折叠组件库，给属性面板更多空间
- 💡 快速选择：只展开节点列表和画布

### 3. 一键重置布局

**按钮位置：** 顶部栏左侧 `重置布局` 按钮

**功能：**
- ✅ 恢复默认宽度比例（20% - 25% - 30% - 25%）
- ✅ 展开所有折叠的面板
- ✅ 即时生效
- ✅ 显示提示信息

**使用场景：**
- 调整混乱后快速恢复
- 重新开始布局设置
- 查看默认推荐布局

### 4. 动态空间分配

**智能算法：**
系统会自动计算并分配空间：
- 折叠的面板固定占用 48px
- 剩余空间按比例分配给展开的面板
- 支持多面板同时折叠

**示例：**
```
默认布局：20% - 25% - 30% - 25%
折叠组件库后：20% - 0% - 50% - 30%（约）
折叠两个后：自动重新分配
```

## 📐 布局预设

### 推荐布局1：标准工作区
```
节点列表(20%) | 组件库(25%) | 画布(30%) | 属性(25%)
适合：正常设计工作
```

### 推荐布局2：大画布模式
```
节点列表(折叠) | 组件库(折叠) | 画布(展开) | 属性(展开)
适合：专注预览和属性调整
```

### 推荐布局3：快速编辑
```
节点列表(展开) | 组件库(展开) | 画布(折叠) | 属性(折叠)
适合：快速选择和添加组件
```

### 推荐布局4：属性专注
```
节点列表(折叠) | 组件库(折叠) | 画布(30%) | 属性(70%)
适合：详细配置组件属性
```

## 🎨 使用技巧

### 快速切换工作模式

**设计模式：**
1. 保持所有面板展开
2. 从组件库拖拽组件
3. 在画布实时预览
4. 调整属性面板配置

**预览模式：**
1. 折叠组件库和属性面板
2. 画布占据大部分空间
3. 专注查看效果
4. 点击组件快速选中

**编辑模式：**
1. 折叠节点列表
2. 展开组件库和画布
3. 展开属性面板
4. 详细调整配置

### 键盘快捷键（计划中）

未来版本将支持：
- ⏳ `Ctrl + 1/2/3/4` - 快速折叠对应面板
- ⏳ `Ctrl + 0` - 重置布局
- ⏳ `Ctrl + =/-` - 调整选中面板宽度

## 🔧 常见问题

**Q: 折叠后找不到按钮？**
A: 折叠按钮始终显示在面板顶部，折叠后会变成 `▶` 箭头。

**Q: 所有面板都能折叠吗？**
A: 是的，所有四个面板都支持折叠。

**Q: 折叠状态会保存吗？**
A: 当前版本不保存，关闭后恢复默认。未来会支持保存布局偏好。

**Q: 如何调整面板宽度？**
A: 当前版本使用固定比例。未来将支持拖拽分隔线调整宽度。

**Q: 重置布局会丢失数据吗？**
A: 不会，只是恢复面板布局，所有节点配置都保留。

**Q: 边框颜色不生效？**
A: 请检查：
- 颜色格式是否正确
- 是否输入了有效的颜色值
- 留空会使用主题色

**Q: 圆角滑块不精确？**
A: 滑块步长为2px，可以获得足够精确的控制。

## 💡 最佳实践

### 不同屏幕尺寸

**小屏幕（<1366px）：**
- 建议折叠不常用的面板
- 专注于一两个主要功能
- 使用重置布局快速恢复

**中等屏幕（1366-1920px）：**
- 使用默认布局即可
- 根据需要临时折叠
- 体验最佳

**大屏幕（>1920px）：**
- 保持所有面板展开
- 充分利用空间
- 可以考虑调整比例

### 工作流程优化

**步骤1：选择节点**
- 保持节点列表展开
- 快速切换不同节点类型

**步骤2：设计布局**
- 展开组件库和画布
- 拖拽添加组件
- 实时查看效果

**步骤3：调整样式**
- 修改边框颜色和圆角
- 在左侧快速编辑区操作
- 画布实时预览

**步骤4：配置属性**
- 展开属性面板
- 可以折叠组件库节省空间
- 详细配置每个组件

## 📊 性能说明

**优化项：**
- ✅ 折叠/展开动画流畅
- ✅ 空间重新分配无延迟
- ✅ 实时预览不卡顿
- ✅ 样式修改即时生效

**建议：**
- 在低配设备上可以折叠不用的面板
- 减少同时显示的内容可以提升性能

## 🆕 未来计划

- ⏳ 拖拽分隔线调整宽度
- ⏳ 保存布局偏好设置
- ⏳ 键盘快捷键支持
- ⏳ 更多布局预设
- ⏳ 面板位置交换
- ⏳ 浮动面板模式

---

**提示：** 布局调整不会影响节点数据，可以自由尝试各种布局方式！
