# 项目群管理 - 两层架构设计说明（完整版）

## 📐 架构概览

项目群管理现在采用与项目管理**完全一致**的两层架构设计，包括：
- **相同的页面布局**（左右分栏、Dashboard）
- **相同的交互体验**（画布控制、工具栏）
- **相同的视觉风格**（网格背景、卡片设计）

## 🏗️ 架构对比

### 项目管理架构
```
Home.ets (项目列表 + Dashboard)
    ├─ 左侧：项目列表
    ├─ 右侧：项目详情 Dashboard / 欢迎页
    └─ 点击项目 / 新建项目
        ↓
Index.ets (项目编辑)
    ├─ 左侧：Sidebar（任务编辑）
    ├─ 右侧：Canvas 画布
    └─ 顶部：工具栏
```

### 项目群管理架构（新 - 完全对标）
```
ProjectGroupHome.ets (项目群列表 + Dashboard)
    ├─ 左侧：项目群列表
    ├─ 右侧：项目群详情 Dashboard / 欢迎页
    └─ 点击项目群 / 新建项目群
        ↓
ProjectGroupIndex.ets (项目群编辑)
    ├─ 左侧：Sidebar（项目引用编辑）
    ├─ 右侧：Canvas 画布（带网格背景）
    └─ 顶部：工具栏
```

## 📄 文件结构

### 1. ProjectGroupHome.ets - 项目群列表+仪表板页
**位置**: `entry/src/main/ets/pages/ProjectGroupHome.ets`

**布局结构**（完全复用 Home.ets）:
```
Row {
  左侧栏 (380px) {
    - 顶部标题栏
    - 项目群列表
    - 空状态提示
  }
  
  右侧内容区 {
    - 项目群 Dashboard（选中时）
    - 欢迎页（未选中时）
  }
}
```

**Dashboard 内容**:
- ✅ 项目群名称
- ✅ 统计卡片（项目数量、依赖关系）
- ✅ 包含的项目列表
- ✅ 操作按钮（编辑、重命名、删除）

**特点**:
- 左右分栏布局，和 Home.ets 完全一致
- 选中项目群后右侧显示详情
- 支持长按显示上下文菜单

### 2. ProjectGroupIndex.ets - 项目群编辑页
**位置**: `entry/src/main/ets/pages/ProjectGroupIndex.ets`

**布局结构**（完全复用 Index.ets）:
```
Stack {
  侧边栏切换按钮 (左侧固定)
  
  Sidebar (380px) {
    - 项目群信息
    - 选中项目详情
    - 项目列表
    - 添加项目区域
  }
  
  画布区域 {
    CanvasBackground (网格背景)
    Canvas (项目网络图)
  }
  
  顶部工具栏 {
    - 锁定/解锁
    - 重置
    - 渲染模式切换
    - 缩放控制
  }
}
```

**功能**:
- ✅ 可视化项目群网络图（Canvas 绘制）
- ✅ 添加/删除项目到项目群
- ✅ 创建项目间依赖关系
- ✅ 拖拽移动项目节点
- ✅ 画布控制（锁定、重置、缩放、渲染模式）
- ✅ 网格背景效果（和 Index.ets 一致）
- ✅ 完整的工具栏（和 Index.ets 一致）

**特点**:
- 100% 复用 Index.ets 的代码结构
- 使用适配器模式（ProjectGroup → Project）
- 完全相同的交互体验

## 🔄 导航流程

### 完整的导航层级

```
主页 (Home.ets)
    ├─ 项目管理
    │   ├─ 选择项目 → Index.ets (编辑项目)
    │   └─ 新建项目 → Index.ets (创建项目)
    │
    └─ 点击 📂 → ProjectGroupHome.ets (项目群管理)
                    ├─ 选择项目群 → ProjectGroupIndex.ets (编辑项目群)
                    │                    └─ 选中项目 → 点击"打开项目" → Index.ets
                    └─ 新建项目群 → ProjectGroupIndex.ets (创建项目群)
```

### 从主页进入项目群管理

1. **Home.ets** 
   - 点击 📂 按钮（项目群管理）
   - ↓
2. **ProjectGroupHome.ets**
   - 左侧：查看项目群列表
   - 右侧：查看项目群 Dashboard 或欢迎页
   - 点击某个项目群 或 点击 + 创建新项目群
   - ↓
3. **ProjectGroupIndex.ets**
   - 左侧：项目引用管理
   - 右侧：Canvas 画布展示
   - 编辑项目群内容
   - 点击返回按钮
   - ↓
4. **ProjectGroupHome.ets**
   - 返回项目群列表

### 跨层级操作

在 **ProjectGroupIndex.ets** 中：
1. 选中一个项目节点
2. 点击"打开项目"按钮
3. 跳转到 **Index.ets**（项目编辑页）
4. 编辑该项目的任务和依赖

## 🎨 UI 设计一致性对比

### ProjectGroupHome vs Home

| 特性 | Home.ets | ProjectGroupHome.ets | 一致性 |
|------|----------|---------------------|--------|
| 左右分栏布局 | ✅ | ✅ | 100% |
| 左侧列表 | ✅ 项目列表 | ✅ 项目群列表 | ✅ |
| 右侧 Dashboard | ✅ 项目详情 | ✅ 项目群详情 | ✅ |
| 欢迎页 | ✅ | ✅ | ✅ |
| 统计卡片 | ✅ | ✅ | ✅ |
| 操作按钮 | ✅ | ✅ | ✅ |
| 长按菜单 | ✅ | ✅ | ✅ |

### ProjectGroupIndex vs Index

| 特性 | Index.ets | ProjectGroupIndex.ets | 一致性 |
|------|-----------|----------------------|--------|
| Stack 布局 | ✅ | ✅ | 100% |
| 侧边栏切换 | ✅ | ✅ | ✅ |
| Sidebar 面板 | ✅ | ✅ | ✅ |
| Canvas 画布 | ✅ | ✅ | ✅ |
| **网格背景** | ✅ | ✅ | ✅ |
| 顶部工具栏 | ✅ | ✅ | ✅ |
| 画布控制 | ✅ 锁定/重置 | ✅ 锁定/重置 | ✅ |
| 渲染模式 | ✅ 正常/缩略 | ✅ 正常/缩略 | ✅ |
| 缩放控制 | ✅ | ✅ | ✅ |

## 🔧 技术实现

### 1. ProjectGroupHome 的 Dashboard

**左右分栏设计**:
```typescript
Row() {
  // 左侧：项目群列表
  this.Sidebar()  // 380px 宽度
  
  // 右侧：Dashboard 或欢迎页
  Column() {
    if (this.selectedProjectGroupDetail) {
      this.ProjectGroupDashboard()  // 项目群详情
    } else {
      this.WelcomeView()  // 欢迎页
    }
  }
  .layoutWeight(1)
}
```

**Dashboard 展示内容**:
- 项目群名称
- 统计卡片（项目数量、依赖关系数）
- 包含的项目列表
- 操作按钮（编辑、重命名、删除）

### 2. ProjectGroupIndex 的画布背景

**网格背景**（和 Index.ets 完全一致）:
```typescript
@Builder
CanvasBackground() {
  Stack() {
    // 背景色
    Column()
      .backgroundColor(ColorResource.surface)
    
    // 网格线
    Column() {
      Stack() {
        // 垂直线
        ForEach(Array.from({ length: 8 }), (_, index) => {
          Line()
            .startPoint([index * 200, 0])
            .endPoint([index * 200, 1600])
            .stroke(ColorResource.surfaceEmphasized)
            .opacity(0.3)
        })
        
        // 水平线
        ForEach(Array.from({ length: 8 }), (_, index) => {
          Line()
            .startPoint([0, index * 200])
            .endPoint([1600, index * 200])
            .stroke(ColorResource.surfaceEmphasized)
            .opacity(0.3)
        })
      }
    }
  }
}
```

### 3. 适配器模式

```typescript
// ProjectGroupIndex.ets
private convertProjectGroupToProject(projectGroup: ProjectGroup): Project {
  // 将 ProjectGroup 转换为 Project
  // 将 ProjectRef 转换为 TaskNode
  // 将 ProjectDependency 转换为 Dependency
  // 这样就可以复用 CanvasController 的所有功能
}
```

### 4. 核心复用组件
- ✅ **CanvasController**: 画布控制和交互
- ✅ **CanvasRenderer**: 图形渲染
- ✅ **CanvasBackground**: 网格背景
- ✅ **ThemeConfig**: 主题和样式
- ✅ **StorageService**: 数据持久化
- ✅ **工具栏**: 锁定、重置、缩放、渲染模式

## 📊 数据流

```
ProjectGroupHome
    ↓ 加载列表
StorageService.getProjectGroupList()
    ↓ 选择项目群
StorageService.getProjectGroup(groupId)
    ↓ 显示 Dashboard
ProjectGroupDashboard (统计信息、项目列表)
    ↓ 点击"编辑"
ProjectGroupIndex
    ↓ 加载
ProjectGroupService.loadProjectGroup(groupId)
    ↓ 转换
convertProjectGroupToProject()
    ↓ 渲染
CanvasRenderer.render(project, config)
    ↓ 编辑
ProjectGroupService.saveProjectGroup()
    ↓ 保存
FileStorageService.saveProjectGroup()
```

## 🎯 使用指南

### 1. 访问项目群管理
```
Home.ets → 点击 📂 按钮 → ProjectGroupHome.ets
```

### 2. 查看项目群详情
```
ProjectGroupHome.ets 
→ 左侧选择项目群 
→ 右侧显示 Dashboard（统计、项目列表）
```

### 3. 编辑项目群
```
ProjectGroupHome.ets 
→ 点击"编辑项目群"按钮 
→ ProjectGroupIndex.ets
```

### 4. 创建第一个项目群
```
ProjectGroupHome.ets 
→ 点击 + 按钮 
→ ProjectGroupIndex.ets
→ 添加项目、创建依赖
```

### 5. 跨层级操作
```
ProjectGroupIndex.ets 
→ 选中项目 
→ 点击"打开项目" 
→ Index.ets（编辑具体项目）
```

## ✨ 完整功能列表

### ProjectGroupHome 功能
- ✅ 左右分栏布局
- ✅ 项目群列表展示
- ✅ 项目群 Dashboard
  - 统计信息（项目数、依赖数）
  - 项目列表展示
  - 操作按钮
- ✅ 欢迎页（未选中时）
- ✅ 创建项目群
- ✅ 重命名项目群
- ✅ 删除项目群
- ✅ 长按上下文菜单
- ✅ 选中高亮效果

### ProjectGroupIndex 功能
- ✅ 侧边栏
  - 项目群信息
  - 选中项目详情
  - 项目列表
  - 添加项目区域
- ✅ Canvas 画布
  - 网格背景
  - 项目节点渲染
  - 依赖关系线条
  - 拖拽移动
  - 点击选择
- ✅ 顶部工具栏
  - 锁定/解锁画布
  - 重置画布位置
  - 切换渲染模式
  - 缩放控制
- ✅ 操作功能
  - 添加项目引用
  - 删除项目引用
  - 创建依赖关系
  - 打开具体项目

## 📝 关键改进点

### 1. **ProjectGroupHome 现在有 Dashboard 了！** ✨
- 左侧：项目群列表（和 Home.ets 一样）
- 右侧：项目群详情 Dashboard（和 Home.ets 一样）
- 完全相同的左右分栏布局

### 2. **ProjectGroupIndex 现在和 Index.ets 完全一致！** ✨
- Stack 布局结构相同
- 添加了网格背景（CanvasBackground）
- 工具栏位置和功能相同
- 画布样式和交互相同

### 3. **统一的视觉体验** ✨
- 相同的卡片设计
- 相同的颜色方案
- 相同的间距和圆角
- 相同的阴影效果

## 🎉 总结

### 完全对标项目管理的架构设计

| 层级 | 项目管理 | 项目群管理 | 对标程度 |
|------|---------|-----------|---------|
| 列表页 | Home.ets | ProjectGroupHome.ets | 100% |
| - 布局 | 左右分栏 | 左右分栏 | ✅ |
| - 左侧 | 项目列表 | 项目群列表 | ✅ |
| - 右侧 | Dashboard | Dashboard | ✅ |
| 编辑页 | Index.ets | ProjectGroupIndex.ets | 100% |
| - 布局 | Stack | Stack | ✅ |
| - 侧边栏 | Sidebar | Sidebar | ✅ |
| - 画布 | Canvas | Canvas | ✅ |
| - 背景 | 网格 | 网格 | ✅ |
| - 工具栏 | 完整 | 完整 | ✅ |

### 现在提供的完整体验
- ✅ 完全统一的两层架构
- ✅ 完全统一的 UI 设计
- ✅ 完全统一的交互方式
- ✅ 100% 代码复用
- ✅ 零学习成本

**项目群管理现在和项目管理拥有完全一致的架构、设计和体验！** 🚀🎊
