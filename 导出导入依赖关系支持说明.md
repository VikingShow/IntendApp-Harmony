# CSV 导出/导入 - 依赖关系支持说明

## 问题描述

用户反馈：导出的 CSV/Excel 文档中缺少依赖关系信息，导致重新导入后节点间没有依赖关系连接。

### 原有问题

**导出 (ExportService)**：
- ✅ 导出任务信息（ID、名称、日期、坐标等）
- ❌ **未导出依赖关系详细信息**
- ❌ 只在统计信息中显示依赖关系数量

**导入 (ImportService)**：
- ✅ 导入任务信息
- ❌ **明确注释"CSV导入不包含依赖关系信息，保持为空"**
- ❌ `project.dependencies = []`（空数组）

## 解决方案

### 整体设计

在 CSV 文件中添加一列 **"前置任务"**，用于存储每个任务的所有前置任务 ID（用分号分隔）。

#### 数据格式示例

| 任务ID | 任务名称 | 开始日期 | 结束日期 | ... | 前置任务 |
|--------|---------|---------|---------|-----|---------|
| task_001 | 需求分析 | 2025-01-01 | 2025-01-05 | ... | |
| task_002 | 设计方案 | 2025-01-06 | 2025-01-10 | ... | task_001 |
| task_003 | 开发实现 | 2025-01-11 | 2025-01-20 | ... | task_001;task_002 |
| task_004 | 测试验证 | 2025-01-21 | 2025-01-25 | ... | task_003 |

**说明**：
- task_002 的前置任务是 task_001
- task_003 的前置任务是 task_001 和 task_002（用分号分隔）
- task_004 的前置任务是 task_003

### 依赖关系的表示

**依赖关系**：`前置任务 -> 当前任务`（fromId -> toId）

在 CSV 中，我们在每一行存储该任务的**所有前置任务 ID**：
- 如果任务 B 依赖任务 A，则在任务 B 的行中记录 A 的 ID
- 如果任务 C 依赖任务 A 和 B，则在任务 C 的行中记录 "A;B"

## 实现细节

### 1. 修改 ExportService.ets

#### 添加"前置任务"列到表头

```typescript
const headers = [
  '任务ID',
  '任务名称',
  '开始日期',
  '结束日期',
  '持续天数',
  '是否关键路径',
  '完成进度',
  '子任务总数',
  '已完成子任务',
  'X坐标',
  'Y坐标',
  '前置任务'  // 新增：依赖关系列
];
```

#### 为每个任务查找并导出前置任务

```typescript
project.tasks.forEach(task => {
  // 查找所有指向该任务的前置任务（fromId -> toId，这个任务是toId）
  const predecessors = project.dependencies
    .filter(dep => dep.toId === task.id)
    .map(dep => dep.fromId);
  
  // 将前置任务ID列表用分号分隔
  const predecessorStr = predecessors.length > 0 ? predecessors.join(';') : '';

  const row = [
    // ... 其他列 ...
    this.escapeCsvValue(predecessorStr)  // 新增：前置任务列表
  ];

  rows.push(row.join(','));
});
```

**关键代码逻辑**：
1. 遍历项目的所有依赖关系 `project.dependencies`
2. 筛选出 `toId === task.id` 的依赖（这些依赖的起点是当前任务的前置任务）
3. 提取所有前置任务的 `fromId`
4. 用分号 `;` 连接多个前置任务 ID
5. 使用 `escapeCsvValue` 处理可能包含特殊字符的值

### 2. 修改 ImportService.ets

#### 2.1 扩展 CsvRowData 接口

```typescript
interface CsvRowData {
  taskId: string;
  taskName: string;
  startDate: string;
  endDate: string;
  duration: string;
  isCritical: string;
  progress: string;
  totalChecklist: string;
  completedChecklist: string;
  x: string;
  y: string;
  predecessors?: string;  // 新增：前置任务列表（用分号分隔的任务ID）
}
```

#### 2.2 修改 parseCSV 方法

**向后兼容**：支持 11 列（旧格式）和 12 列（新格式）

```typescript
// 需要至少11列数据（兼容旧格式），12列为新格式（包含前置任务）
if (columns.length >= 11) {
  rows.push({
    taskId: columns[0],
    taskName: columns[1],
    startDate: columns[2],
    endDate: columns[3],
    duration: columns[4],
    isCritical: columns[5],
    progress: columns[6],
    totalChecklist: columns[7],
    completedChecklist: columns[8],
    x: columns[9],
    y: columns[10],
    predecessors: columns.length >= 12 ? columns[11] : undefined  // 新增：前置任务列（可选）
  });
}
```

#### 2.3 修改 convertCSVToProject 方法

**核心改进**：建立 ID 映射并重建依赖关系

```typescript
// 建立原始ID到新ID的映射
const oldIdToNewId = new Map<string, string>();

// 转换任务列表
project.tasks = rows.map((row, index) => {
  // ... 解析任务数据 ...
  
  // 生成新的任务ID
  const newTaskId = `task_${Date.now()}_${index}`;
  
  // 记录原始ID到新ID的映射
  if (row.taskId) {
    oldIdToNewId.set(row.taskId, newTaskId);
  }
  
  // ... 创建任务对象 ...
});

// 重建依赖关系
project.dependencies = [];

rows.forEach((row, index) => {
  // 如果该行包含前置任务信息
  if (row.predecessors && row.predecessors.trim().length > 0) {
    const currentTaskId = project.tasks[index].id;
    
    // 前置任务ID列表（用分号分隔）
    const predecessorIds = row.predecessors.split(';')
      .map(id => id.trim())
      .filter(id => id.length > 0);
    
    // 为每个前置任务创建依赖关系
    predecessorIds.forEach(oldPredecessorId => {
      // 将旧ID映射到新ID
      const newPredecessorId = oldIdToNewId.get(oldPredecessorId);
      
      if (newPredecessorId) {
        // 创建依赖关系：前置任务 -> 当前任务
        const dependency: Dependency = {
          fromId: newPredecessorId,
          toId: currentTaskId
        };
        
        project.dependencies.push(dependency);
      }
    });
  }
});
```

**关键步骤**：

1. **ID 映射表**：
   - 导入时会生成新的任务 ID
   - 使用 `Map<string, string>` 记录 `原始ID -> 新ID` 的映射

2. **解析前置任务**：
   - 读取 `predecessors` 字段（格式：`"id1;id2;id3"`）
   - 按分号分割得到前置任务 ID 数组
   - 过滤空值

3. **重建依赖关系**：
   - 遍历每个前置任务 ID
   - 通过映射表将原始 ID 转换为新 ID
   - 创建 `Dependency` 对象：`{ fromId: 前置任务ID, toId: 当前任务ID }`
   - 添加到 `project.dependencies` 数组

4. **错误处理**：
   - 如果找不到前置任务的新 ID 映射，输出警告日志
   - 继续处理其他依赖关系，不中断导入流程

## 技术要点

### 1. 为什么用分号分隔？

- **CSV 格式限制**：列值用逗号分隔，不能在值中使用逗号
- **分号**：在任务 ID 中不常出现，适合作为分隔符
- **可读性**：`"task_001;task_002"` 比其他分隔符更易读

### 2. ID 映射的必要性

**问题**：导入时会生成新的任务 ID，不能直接使用 CSV 中的原始 ID

**解决**：
```typescript
// 原始 CSV
task_001 -> task_002  // task_002 依赖 task_001

// 导入后
task_1738234567_0 -> task_1738234567_1  // 新ID

// 映射表
{
  "task_001": "task_1738234567_0",
  "task_002": "task_1738234567_1"
}
```

### 3. 向后兼容性

**旧格式（11 列）**：
```csv
任务ID,任务名称,...,Y坐标
task_001,需求分析,...,100
```

**新格式（12 列）**：
```csv
任务ID,任务名称,...,Y坐标,前置任务
task_001,需求分析,...,100,
task_002,设计方案,...,150,task_001
```

**兼容策略**：
- 检查 `columns.length >= 11` 确保基本字段存在
- 如果 `columns.length >= 12`，读取前置任务字段
- 否则，`predecessors` 为 `undefined`，不创建依赖关系

## 测试场景

### 场景 1：简单线性依赖

```
A -> B -> C
```

**CSV**：
| 任务ID | 任务名称 | ... | 前置任务 |
|--------|---------|-----|---------|
| A | 任务A | ... | |
| B | 任务B | ... | A |
| C | 任务C | ... | B |

**验证**：导入后应有 2 个依赖关系（A->B, B->C）

### 场景 2：多个前置任务

```
A ─┐
   ├─> C
B ─┘
```

**CSV**：
| 任务ID | 任务名称 | ... | 前置任务 |
|--------|---------|-----|---------|
| A | 任务A | ... | |
| B | 任务B | ... | |
| C | 任务C | ... | A;B |

**验证**：导入后应有 2 个依赖关系（A->C, B->C）

### 场景 3：复杂网络

```
A ─┬─> C ─> E
   │         ↑
B ─┴────> D ─┘
```

**CSV**：
| 任务ID | 任务名称 | ... | 前置任务 |
|--------|---------|-----|---------|
| A | 任务A | ... | |
| B | 任务B | ... | |
| C | 任务C | ... | A;B |
| D | 任务D | ... | B |
| E | 任务E | ... | C;D |

**验证**：导入后应有 5 个依赖关系（A->C, B->C, B->D, C->E, D->E）

### 场景 4：向后兼容（旧格式）

**CSV（11列，无前置任务列）**：
| 任务ID | 任务名称 | ... | Y坐标 |
|--------|---------|-----|------|
| A | 任务A | ... | 100 |
| B | 任务B | ... | 150 |

**验证**：
- ✅ 成功导入 2 个任务
- ✅ 依赖关系为空（0 个）
- ✅ 无错误或警告

## 日志输出

### 导出日志

```
[ExportService] 开始导出 Excel/CSV...
[ExportService] 导出成功: 文件路径
```

### 导入日志

```
[ImportService] 开始解析 CSV 数据...
[ImportService] 创建依赖关系: task_1738234567_0 -> task_1738234567_2
[ImportService] 创建依赖关系: task_1738234567_1 -> task_1738234567_2
[ImportService] CSV转换完成: 3 个任务, 2 个依赖关系
```

### 错误日志（如果前置任务ID无效）

```
[ImportService] 警告：找不到前置任务ID映射: invalid_task_id
```

## 注意事项

### 1. CSV 转义

使用 `escapeCsvValue` 方法处理特殊字符：
- 包含逗号、引号或换行符的值会被引号包裹
- 内部的引号会被转义为 `""`

### 2. 任务顺序

CSV 中的任务顺序很重要：
- ✅ **前置任务必须在当前任务之前出现**
- ❌ 如果前置任务在后面，ID 映射表中找不到映射

**推荐**：导出时按拓扑顺序排序任务（可选优化）

### 3. 性能考虑

- **时间复杂度**：O(n × m)，其中 n 是任务数，m 是平均每个任务的前置任务数
- **空间复杂度**：O(n)，用于 ID 映射表
- **优化空间**：对于大型项目（1000+ 任务），可考虑使用 Set 加速查找

## 功能验证清单

- [x] ✅ ExportService 导出时包含"前置任务"列
- [x] ✅ ImportService 解析"前置任务"列
- [x] ✅ 重建依赖关系（ID 映射正确）
- [x] ✅ 向后兼容旧格式（11列）
- [x] ✅ 处理多个前置任务（分号分隔）
- [x] ✅ 处理空前置任务（起始节点）
- [x] ✅ 错误处理（无效前置任务ID）
- [x] ✅ 日志输出（调试信息）
- [x] ✅ 无编译错误

## 用户体验改进

**之前**：
1. 导出项目为 CSV
2. 重新导入 CSV
3. ❌ **所有依赖关系丢失，需要手动重新连接**

**现在**：
1. 导出项目为 CSV
2. 重新导入 CSV
3. ✅ **依赖关系自动恢复，完整保留项目结构**

## 后续优化建议

### 1. 拓扑排序

在导出时按依赖顺序排序任务，确保前置任务总是在前面：

```typescript
// 伪代码
function topologicalSort(tasks: TaskNode[], dependencies: Dependency[]): TaskNode[] {
  // 使用 Kahn 算法或 DFS 进行拓扑排序
}
```

### 2. 循环依赖检测

在导入时检测并警告循环依赖：

```typescript
function detectCycle(dependencies: Dependency[]): boolean {
  // 使用 DFS 检测环
}
```

### 3. 任务名称作为备选标识

如果 ID 映射失败，尝试通过任务名称匹配：

```typescript
if (!newPredecessorId) {
  // 尝试通过名称查找
  const taskByName = project.tasks.find(t => t.name === oldPredecessorName);
  if (taskByName) {
    newPredecessorId = taskByName.id;
  }
}
```

---

**修复完成时间**：2025-10-13  
**修复状态**：✅ 已完成，无编译错误  
**修改文件**：
- `entry/src/main/ets/services/ExportService.ets`（generateCSV 方法）
- `entry/src/main/ets/services/ImportService.ets`（CsvRowData 接口、parseCSV 方法、convertCSVToProject 方法）

