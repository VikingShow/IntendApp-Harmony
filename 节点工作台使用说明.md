# 节点工作台使用说明

## 📋 功能概览

节点工作台是一个强大的节点类型编辑器，支持可视化配置节点卡片的样式和布局。

## 🎨 界面布局

### 四栏式布局

```
┌─────────┬──────────┬────────────┬──────────┐
│节点类型 │ 组件库   │ 可视化画布 │ 属性面板 │
│列表(20%)│ (25%)   │   (30%)   │  (25%)  │
└─────────┴──────────┴────────────┴──────────┘
```

#### 1. 节点类型列表（左侧）
- **搜索框**：快速查找节点类型
- **筛选器**：全部/已发布/草稿
- **节点列表**：显示所有节点类型
- **操作按钮**：
  - `+ 新建类型`：创建新的节点类型
  - `发布/存为草稿`：切换发布状态
  - `删除`：删除当前节点类型

#### 2. 组件库面板（中左）
- 展示10种可用组件
- 分类筛选：基础/数据/高级
- **点击组件**即可添加到画布

#### 3. 可视化画布（中右）
- **实时预览**节点卡片效果
- **点击组件**进行选中
- **删除按钮**（右上角 ×）

#### 4. 属性编辑面板（右侧）
- 显示选中组件的详细信息
- 编辑组件特定属性

## 🔄 视图模式说明

### ⚠️ 视图切换功能已移除

**原因：** 为了提供更一致的用户体验，目前只保留了**可视化编辑模式**。

**可视化模式的优势：**
- ✅ 所见即所得（WYSIWYG）
- ✅ 拖拽式操作，直观易用
- ✅ 实时预览节点效果
- ✅ 智能属性面板

**经典模式（已废弃）** 是基于表单的编辑方式，需要在多个Tab间切换，体验不如可视化模式流畅。

## 📥 导入/导出功能

### 导出JSON
**位置：** 顶部栏 → `导出JSON` 按钮

**功能：**
- 导出当前所有节点类型的JSON配置
- 用于备份或分享节点类型

### 导入JSON（新功能✨）
**位置：** 顶部栏 → `导入JSON` 按钮

**使用步骤：**
1. 点击 `导入JSON` 按钮
2. 在弹出的对话框中粘贴节点类型的JSON数据
3. 点击 `导入` 按钮

**导入规则：**
- ✅ 自动验证JSON格式
- ✅ 检查必要字段（id, name, style, layout）
- ✅ 如ID已存在，将覆盖旧版本
- ❌ 格式错误将显示提示信息

**JSON格式示例：**
```json
{
  "id": "custom_task",
  "version": 1,
  "name": "自定义任务",
  "description": "自定义的任务节点类型",
  "style": {
    "fillColor": "#007AFF",
    "borderRadius": 16
  },
  "fields": [],
  "layout": [
    {
      "id": "title_1",
      "type": "TITLE"
    },
    {
      "id": "date_1",
      "type": "DATE_RANGE"
    }
  ],
  "meta": {},
  "ports": {},
  "rules": [],
  "isSystem": false,
  "isDraft": true
}
```

## 🎯 快速操作指南

### 创建新节点类型
1. 点击左侧列表底部的 `+ 新建类型`
2. 新节点类型会自动创建并选中
3. 开始编辑节点属性

### 添加组件到节点
1. 在组件库中点击想要的组件
2. 组件会自动添加到画布底部
3. 点击组件可以编辑属性

### 调整组件顺序
**当前方式：** 删除后重新添加
**未来支持：** 拖拽排序（开发中）

### 删除组件
1. 在画布中点击要删除的组件
2. 点击组件右上角的 `×` 按钮

### 发布节点类型
1. 选中要发布的节点类型
2. 点击左侧列表底部的 `发布` 按钮
3. 节点类型状态从"草稿"变为"已发布"

### 删除节点类型
1. 选中要删除的节点类型
2. 点击左侧列表底部的 `删除` 按钮
3. 在确认对话框中点击 `删除`

**注意：** 系统内置节点类型无法删除

## 💾 自动保存

节点工作台支持**自动保存**功能：
- ✅ 编辑后自动保存到本地存储
- ✅ 关闭应用后数据不会丢失
- ✅ 也可手动点击 `保存` 按钮

## 🎨 可用组件类型

### 基础组件
- **TITLE** - 标题：显示任务名称
- **DATE_RANGE** - 日期范围：显示开始/结束日期
- **STATUS_DOT** - 状态点：彩色状态指示器

### 数据展示组件
- **TIME_PROGRESS** - 时间进度：蓝色进度条
- **TASK_PROGRESS** - 任务进度：绿色进度条
- **CHECKLIST_SUMMARY** - 清单统计：完成情况统计
- **PRIORITY_BADGE** - 优先级：优先级标签

### 高级组件
- **ASSIGNEE** - 负责人：显示任务负责人
- **TAGS** - 标签：多标签显示
- **CUSTOM_FIELD** - 自定义字段：需要先创建字段并绑定

## ⚙️ 组件属性配置

### CUSTOM_FIELD（自定义字段）
**必须先创建字段：**
1. 在"属性"Tab的"字段管理"中创建字段
2. 添加CUSTOM_FIELD组件到画布
3. 在属性面板中绑定字段

### TITLE / TAGS / ASSIGNEE
可配置 **最大行数**（1-3行）

### 其他组件
使用默认样式，无需额外配置

## 🔍 搜索和筛选

### 搜索节点类型
在左侧列表顶部的搜索框中输入关键词：
- 支持按名称搜索
- 支持按描述搜索
- 支持按ID搜索

### 筛选节点类型
点击筛选标签：
- **全部**：显示所有节点类型
- **已发布**：只显示已发布的类型
- **草稿**：只显示草稿状态的类型

## 📊 节点样式配置

在属性面板中可以配置：
- **主题色**：节点边框颜色
- **边框色**：可选，覆盖主题色
- **圆角半径**：0-32px
- **关键任务色**：用于关键路径

## ⚠️ 常见问题

**Q: 自定义字段组件显示"未绑定字段"？**
A: 需要先在"属性"Tab的"字段管理"中创建字段。

**Q: 删除按钮不见了？**
A: 请先选中一个节点类型，底部才会显示删除按钮。

**Q: 导入JSON失败？**
A: 检查JSON格式是否正确，必须包含id、name、style、layout字段。

**Q: 组件顺序如何调整？**
A: 目前需要删除后重新添加。拖拽排序功能正在开发中。

**Q: 视图切换按钮去哪了？**
A: 为了提供更好的体验，已统一使用可视化编辑模式。

## 🚀 最佳实践

### 推荐的组件布局
```
TITLE（标题）
↓
DATE_RANGE（日期范围）
↓
TIME_PROGRESS（时间进度）
↓
TASK_PROGRESS（任务进度）
↓
TAGS / PRIORITY_BADGE（标签/优先级）
```

### 性能建议
- 避免添加过多组件（建议 ≤ 8个）
- 自定义字段需要实际数据支持
- 定期导出JSON作为备份

## 📝 更新日志

### v1.1.2（当前版本）
- ✅ 新增可视化拖拽编辑器
- ✅ 新增组件库面板
- ✅ 新增属性编辑面板
- ✅ 新增导入JSON功能
- ✅ 恢复节点类型操作按钮
- ✅ 优化四栏布局

### 未来计划
- ⏳ 拖拽排序组件
- ⏳ 组件复制功能
- ⏳ 撤销/重做
- ⏳ 预设模板库

---

如有问题或建议，请联系开发团队。
