# 项目地图功能说明

## 功能概述

**项目地图**是EasyFlow的新增功能，允许您在更高的层级上管理多个项目之间的关系。与现有的任务画布类似，项目地图将每个项目作为一个节点进行可视化展示，您可以在项目之间建立连接关系。

## 主要特性

### 1. 项目节点可视化
- **自动布局**：新项目自动按网格排列
- **拖拽移动**：可以自由拖动项目节点调整位置
- **项目信息**：每个节点显示项目名称和基本信息

### 2. 项目连接
- **建立关联**：通过拖拽可以在项目之间建立连接线
- **关系管理**：清晰展示项目之间的依赖或关联关系
- **智能验证**：防止自连接等无效操作

### 3. 画布操作
- **缩放控制**：支持放大、缩小和重置视图
- **平移拖动**：可以拖动整个画布查看全局
- **画布锁定**：锁定后防止误操作
- **状态保存**：自动保存画布位置和缩放比例

## 使用方法

### 进入项目地图

1. **平板/大屏设备**：
   - 在首页左侧边栏顶部，找到应用图标按钮（蓝色圆形）
   - 点击即可进入项目地图视图

2. **手机设备**：
   - 上滑进入项目列表页面
   - 在顶部标题栏右侧，找到应用图标按钮
   - 点击即可进入项目地图视图

### 基本操作

#### 查看项目节点
- 项目地图会自动显示所有现有项目
- 每个节点显示项目名称
- 节点按网格自动排列

#### 移动节点
1. 长按项目节点
2. 拖动到目标位置
3. 松开即可完成移动
4. 位置会自动保存

#### 建立连接
1. 点击工具栏的"连接"按钮（或长按节点）
2. 选择起始项目节点
3. 点击目标项目节点
4. 系统自动创建连接线

#### 画布操作
- **缩放**：点击工具栏的🔍按钮，使用+/-控制缩放
- **重置**：点击🔄按钮恢复默认视图
- **锁定**：点击🔒按钮锁定画布，防止误操作
- **返回**：点击左上角<按钮返回首页

## 数据存储

### 存储位置
项目地图数据保存在应用私有目录：
```
/data/storage/el2/base/haps/entry/files/project_map.json
```

### 数据结构
```typescript
{
  "id": "project_map",
  "nodes": [
    {
      "id": "project_xxx",
      "name": "项目名称",
      "x": 100,
      "y": 100,
      "lastModified": 1234567890
    }
  ],
  "connections": [
    {
      "fromId": "project_1",
      "toId": "project_2"
    }
  ],
  "canvasOffsetX": 0,
  "canvasOffsetY": 0,
  "zoomScale": 1.0
}
```

### 自动同步
- 系统自动同步项目列表到地图节点
- 新建项目自动添加到地图
- 删除项目自动从地图移除
- 节点名称自动与项目名称同步

## 技术实现

### 核心组件

1. **ProjectMapData.ets**
   - 项目地图数据模型
   - 定义节点和连接结构

2. **ProjectMapService.ets**
   - 项目地图服务
   - 负责数据存储和加载
   - 提供同步和操作接口

3. **ProjectMapView.ets**
   - 项目地图视图页面
   - 复用CanvasController和CanvasRenderer
   - 实现节点渲染和交互

### 复用机制
- **画布组件**：复用任务画布的渲染引擎
- **控制器**：复用CanvasController处理交互
- **渲染器**：复用CanvasRenderer绘制图形
- **工具栏**：复用缩放、锁定等控制组件

### 设计模式
- **单例模式**：ProjectMapService确保唯一实例
- **服务模式**：分离数据管理和UI逻辑
- **适配器模式**：将ProjectMapData转换为Project格式以复用渲染器

## 应用场景

1. **项目组合管理**
   - 管理多个相关项目的关系
   - 可视化项目依赖结构

2. **项目分类整理**
   - 按业务线组织项目
   - 建立项目间的逻辑关系

3. **大局观把握**
   - 从更高视角查看所有项目
   - 理解项目间的关联和影响

## 未来扩展

### 计划功能
1. **项目分组**：支持项目分组和层级管理
2. **连接类型**：支持不同类型的连接关系
3. **筛选显示**：按分类或状态筛选项目
4. **导出地图**：导出项目关系图为图片
5. **批量操作**：支持批量移动和连接

### 优化方向
1. **性能优化**：大量项目时的渲染性能
2. **布局算法**：智能自动布局算法
3. **交互增强**：更多手势和快捷操作
4. **数据分析**：基于项目关系的统计分析

## 注意事项

1. **数据安全**
   - 项目地图数据独立存储
   - 不影响原有项目数据
   - 删除项目会同步清理地图数据

2. **性能考虑**
   - 建议项目数量控制在50个以内以保证流畅体验
   - 连接数量建议不超过100条

3. **兼容性**
   - 完全兼容现有功能
   - 不影响任务画布的使用
   - 可独立开关使用

## 反馈与支持

如有问题或建议，请通过以下方式反馈：
- 提交Issue到项目仓库
- 联系开发团队
- 查看项目文档

---

**版本信息**
- 功能版本：v1.1.0
- 更新日期：2025-10
- 开发团队：EasyFlow Team


