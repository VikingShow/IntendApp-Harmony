# 节点可视化编辑器使用指南

## 概述

节点工作台现已升级为类 Axure RP 的可视化拖拽编辑器，支持所见即所得的节点卡片设计。

## 新架构 - 四栏布局

### 1. **节点类型列表**（左侧 20%）
- 显示所有节点类型（系统内置 + 自定义）
- 支持搜索和筛选（全部/已发布/草稿）
- 点击选择要编辑的节点类型

### 2. **组件库面板**（中左 25%）
- 展示 10 种可用组件类型：
  - **基础组件**：标题、日期范围、状态点
  - **数据组件**：时间进度、任务进度、清单统计、优先级
  - **高级组件**：负责人、标签、自定义字段
- 支持分类筛选
- 点击组件快速添加到画布底部

### 3. **可视化画布**（中右 30%）
- 实时预览节点卡片效果
- 显示真实的节点样式（边框色、圆角等）
- 展示所有已添加的组件
- 点击组件进行选中
- 选中组件显示删除按钮（右上角 ×）

### 4. **属性编辑面板**（右侧 25%）
- 显示选中组件的详细信息
- 编辑组件特定属性：
  - 自定义字段：绑定到已创建的字段
  - 标题/标签/负责人：设置最大行数
  - 其他组件：使用默认配置

## 使用流程

### 快速开始

1. **选择节点类型**
   - 在左侧列表中点击要编辑的节点类型
   - 或点击"+ 新建"创建新类型

2. **添加组件到画布**
   - 从组件库点击组件，自动添加到画布底部
   - 或长按拖拽到指定位置（开发中）

3. **编辑组件属性**
   - 在画布中点击组件进行选中
   - 右侧属性面板显示可编辑属性
   - 修改后自动保存

4. **删除组件**
   - 选中组件后，点击右上角的 × 按钮

### 组件类型说明

#### TITLE - 标题
- 显示任务名称
- 通常放在卡片顶部
- 支持设置最大行数

#### DATE_RANGE - 日期范围
- 显示开始/结束日期和工期
- 格式：`12/23 ~ 12/30 (8天)`

#### TIME_PROGRESS - 时间进度
- 基于当前时间计算的进度条
- 蓝色进度条 + 百分比

#### TASK_PROGRESS - 任务进度
- 基于清单完成情况的进度条
- 绿色进度条 + 百分比

#### CHECKLIST_SUMMARY - 清单统计
- 显示清单完成情况
- 格式：`3/5 项已完成`

#### PRIORITY_BADGE - 优先级
- 显示优先级标签
- 用于突出重要任务

#### ASSIGNEE - 负责人
- 显示任务负责人信息

#### TAGS - 标签
- 显示多个标签
- 彩色标签样式

#### STATUS_DOT - 状态点
- 彩色圆点 + 状态文本
- 通过颜色区分状态

#### CUSTOM_FIELD - 自定义字段
- 显示自定义字段的值
- **需要先在字段管理中创建字段**
- 在属性面板中绑定字段

## 实际节点结构

当前实现基于 Canvas 渲染的真实节点卡片结构：

```typescript
// 节点尺寸
const NODE_WIDTH = 200;
const NODE_HEIGHT = 120;

// 默认布局（从上到下）
1. 标题（任务名称）- 16px 粗体
2. 日期范围 - 11px 灰色
3. 时间进度条 - 8px 高度，蓝色
4. 任务进度条 - 8px 高度，绿色
```

## 样式配置

### 节点样式
- **主题色**：用作边框颜色
- **边框色**（可选）：覆盖主题色
- **圆角半径**：0-32px
- **关键任务色**：用于关键路径/高优先级任务

### 样式实时预览
画布中的节点卡片会实时反映样式配置：
- 边框颜色和粗细
- 圆角半径
- 组件排列顺序

## 高级功能

### 字段管理集成
1. 在"属性"Tab 中创建自定义字段
2. 添加 CUSTOM_FIELD 组件到画布
3. 在属性面板中绑定字段
4. 字段值会在实际任务节点中显示

### 组件排序（即将支持）
- 拖拽组件调整顺序
- 画布中显示拖放指示器

### 组件复制（规划中）
- 快速复制现有组件
- 保留所有属性配置

## 最佳实践

### 推荐的组件布局
```
标题 (TITLE)
↓
日期范围 (DATE_RANGE)
↓
时间进度 (TIME_PROGRESS)
↓
任务进度 (TASK_PROGRESS)
↓
标签/优先级 (TAGS / PRIORITY_BADGE)
```

### 性能优化
- 避免添加过多组件（建议 ≤ 8 个）
- 自定义字段需要实际数据支持
- 使用自动保存，无需频繁手动保存

### 常见问题

**Q: 自定义字段组件显示"未绑定字段"？**
A: 需要先在"属性"Tab 的"字段管理"中创建字段，然后在属性面板中绑定。

**Q: 如何调整组件顺序？**
A: 目前需要删除后重新添加。拖拽排序功能正在开发中。

**Q: 画布预览和实际渲染有差异？**
A: 画布预览是简化版示例数据，实际节点会根据任务真实数据渲染。

**Q: 可以导入/导出节点配置吗？**
A: 可以！点击顶部"导出JSON"按钮，或在 JSON Tab 中编辑。

## 技术实现

### 新增组件文件
```
components/
├── NodeComponentRenderer.ets   # 组件渲染器
├── NodeVisualCanvas.ets        # 可视化画布
├── ComponentLibrary.ets        # 组件库面板
└── ComponentPropertyPanel.ets  # 属性编辑面板
```

### 数据模型
```typescript
interface NodeComponentSchema {
  id: string;                    // 组件唯一ID
  type: NodeComponentType;       // 组件类型
  fieldId?: string;              // 绑定的字段ID
  props?: NodeComponentProps;    // 扩展属性
}
```

### 状态管理
- `selectedSchemaId`: 当前选中的节点类型
- `selectedComponentId`: 当前选中的组件
- `currentViewMode`: 视图模式（可视化/经典）

## 未来规划

- ✅ 可视化拖拽编辑器
- ✅ 实时预览
- ✅ 组件库系统
- ⏳ 拖拽排序
- ⏳ 组件复制
- ⏳ 撤销/重做
- ⏳ 预设模板
- ⏳ 组件分组

## 反馈

如有问题或建议，请反馈给开发团队。
