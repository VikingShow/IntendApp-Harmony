# 问题解决说明

## 问题描述

1. **Sidebar一直显示"正在加载项目"**：初次进入app时，进入项目编辑页，sidebar会一直显示正在加载项目，只有在关闭sidebar后再打开才会显示信息。

2. **左侧编辑时右侧不同步更新**：在左侧编辑sidebar中的节点信息时，右侧画布不会实时同步更新。

## 问题分析

### 问题1的根本原因
- Sidebar组件使用的是`@ObjectLink project: Project | null`，但是ProjectManager的状态更新后，Sidebar没有正确响应状态变化。
- Index.ets中没有使用`@State`来管理project和selectedTask的状态，导致UI无法响应状态变化。

### 问题2的根本原因
- Canvas的渲染没有监听ProjectManager的状态变化，只在初始化时渲染一次。
- 缺少事件驱动的状态同步机制。

## 解决方案

### 1. 修复状态管理问题

**在Index.ets中**：
```typescript
// 使用@State来响应ProjectManager的状态变化
@State project: Project | null = null;
@State selectedTask: TaskNode | null = null;

// 添加状态更新方法
private updateStateFromManager() {
  const state = this.projectManager.getState();
  this.project = state.project;
  this.selectedTask = state.selectedTask;
}
```

**在Sidebar.ets中**：
```typescript
// 使用@ObjectLink来响应状态变化
@ObjectLink project: Project | null;
@ObjectLink selectedTask: TaskNode | null;

// 改进aboutToUpdate方法
aboutToUpdate() {
  // 当selectedTask变化时，更新selectedTaskForEditor
  if (this.selectedTaskForEditor?.id !== this.selectedTask?.id) {
    this.selectedTaskForEditor = this.selectedTask;
  }
}
```

### 2. 实现事件驱动的状态同步

**在ProjectManager.ets中**：
```typescript
// 在状态变化时发送事件
private async loadProject(projectId: string): Promise<void> {
  // ... 加载逻辑
  this.eventBus.emit(Events.PROJECT_LOADED, project);
}

private updateTask(task: TaskNode): void {
  // ... 更新逻辑
  this.eventBus.emit(Events.TASK_UPDATED, task);
  this.eventBus.emit(Events.PROJECT_SAVED, this.state.project);
}
```

**在Index.ets中**：
```typescript
// 设置事件监听器
private setupEventListeners() {
  const eventBus = this.projectManager['eventBus'];
  
  // 监听项目加载事件
  eventBus.on('project_loaded', (project: Project) => {
    this.updateStateFromManager();
    this.drawGraph();
  });
  
  // 监听任务更新事件
  eventBus.on('task_updated', (task: TaskNode) => {
    this.updateStateFromManager();
    this.drawGraph();
  });
  
  // 其他事件监听...
}
```

### 3. 优化回调函数

简化Sidebar的回调函数，移除重复的状态更新调用：
```typescript
onUpdateTask: (task) => {
  this.projectManager.getActions().updateTask(task);
  // 事件监听器会自动处理状态更新和画布重绘
},
```

## 解决效果

### 1. Sidebar加载问题解决
- ✅ 初次进入app时，Sidebar能正确显示项目信息
- ✅ 不需要关闭再打开Sidebar就能看到内容
- ✅ 项目状态变化时，Sidebar能实时响应

### 2. 实时同步问题解决
- ✅ 左侧编辑节点信息时，右侧画布实时更新
- ✅ 任务选择、更新、删除等操作都能实时同步
- ✅ 关键路径计算后能立即在画布上显示

## 技术要点

### 1. ArkTS状态管理
- 使用`@State`装饰器管理组件状态
- 使用`@ObjectLink`实现父子组件状态同步
- 正确使用`aboutToUpdate`生命周期方法

### 2. 事件驱动架构
- 使用EventBus实现组件间解耦通信
- 在状态变化时发送事件通知
- 通过事件监听器实现响应式更新

### 3. 避免重复更新
- 移除手动调用状态更新的代码
- 统一通过事件监听器处理状态变化
- 确保状态更新的单一数据流

## 注意事项

1. **状态管理**：确保所有状态变化都通过ProjectManager进行
2. **事件监听**：在组件销毁时清理事件监听器
3. **性能优化**：避免不必要的事件发送和状态更新
4. **错误处理**：在事件处理中添加适当的错误处理

## 总结

通过这次修复，项目实现了：
- **响应式状态管理**：UI能正确响应状态变化
- **实时数据同步**：左侧编辑能实时反映到右侧画布
- **事件驱动架构**：组件间通过事件进行解耦通信
- **更好的用户体验**：消除了加载延迟和同步问题

这些改进确保了应用的状态管理和UI同步功能正常工作，提供了流畅的用户体验。
