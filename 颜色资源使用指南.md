# 颜色资源使用指南

## 概述

项目现在提供三种方式来使用颜色：

1. **ColorResource 类** - 使用 Resource 类型，从 `color.json` 读取，支持深色模式自动切换（推荐）
2. **ResponsiveColorConfig 类** - 响应式颜色配置，支持设备类型和深色模式自动适配（手机端推荐）
3. **ThemeConfig 类** - 使用字符串类型，用于需要硬编码颜色值的场景（特殊场景）

## 推荐使用方式

### ✅ 推荐：使用 ColorResource（支持深色模式）

```typescript
import { ColorResource } from '../utils/ThemeConfig';

@Entry
@Component
struct MyComponent {
  build() {
    Column() {
      Text('标题文字')
        .fontColor(ColorResource.textPrimary)
        .backgroundColor(ColorResource.backgroundCard)
      
      Button('主要按钮')
        .backgroundColor(ColorResource.primaryColor)
        .fontColor(ColorResource.surface)
      
      Button('成功按钮')
        .backgroundColor(ColorResource.successColor)
        .fontColor(ColorResource.surface)
      
      Text('次要文字')
        .fontColor(ColorResource.textSecondary)
        .backgroundColor(ColorResource.backgroundSecondary)
    }
    .backgroundColor(ColorResource.backgroundPage)
    .borderColor(ColorResource.borderDefault)
  }
}
```

### 使用 ThemeConfig（仅浅色模式）

用于需要字符串类型颜色值的特殊场景：

```typescript
import { ThemeConfig } from '../utils/ThemeConfig';

// 在 Canvas 绘制等需要字符串的场景
context.fillStyle = ThemeConfig.primaryColor;
context.strokeStyle = ThemeConfig.borderDefault;
```

## ColorResource 完整颜色列表

### 主色调
- `ColorResource.primaryColor` - 主色 (#007AFF)
- `ColorResource.primaryLight` - 浅主色 (#E3F2FD)
- `ColorResource.primaryDark` - 深主色 (#0056CC)
- `ColorResource.primaryUltraLight` - 超浅主色 (#F0F8FF)

### 辅助色
- `ColorResource.secondaryColor` - 辅助色 (#8E8E93)
- `ColorResource.secondaryLight` - 浅辅助色 (#F2F2F7)
- `ColorResource.secondaryDark` - 深辅助色 (#1C1C1E)

### 功能色
- `ColorResource.successColor` - 成功色 (#34C759)
- `ColorResource.successLight` - 浅成功色 (#E8F5E8)
- `ColorResource.warningColor` - 警告色 (#FF9500)
- `ColorResource.warningLight` - 浅警告色 (#FFF4E6)
- `ColorResource.errorColor` - 错误色 (#FF3B30)
- `ColorResource.errorLight` - 浅错误色 (#FFEBEE)
- `ColorResource.infoColor` - 信息色 (#5AC8FA)
- `ColorResource.infoLight` - 浅信息色 (#E3F2FD)

### 表面色系统（11级灰度）
- `ColorResource.surface` - 表面色 (#FFFFFF)
- `ColorResource.surfaceSubtle` - 微妙表面 (#F9FAFB)
- `ColorResource.surfaceLight` - 浅表面 (#F2F2F7)
- `ColorResource.surfaceNeutral` - 中性表面 (#E5E5EA)
- `ColorResource.surfaceMuted` - 柔和表面 (#D1D1D6)
- `ColorResource.surfaceStrong` - 强表面 (#C7C7CC)
- `ColorResource.surfaceEmphasized` - 强调表面 (#8E8E93)
- `ColorResource.surfaceBold` - 粗体表面 (#636366)
- `ColorResource.surfaceDark` - 暗表面 (#48484A)
- `ColorResource.surfaceDeep` - 深表面 (#3A3A3C)
- `ColorResource.surfaceDeepest` - 最深表面 (#1C1C1E)

### 背景色系统
- `ColorResource.backgroundPrimary` - 主要背景 (#FFFFFF)
- `ColorResource.backgroundSecondary` - 次要背景 (#F2F2F7)
- `ColorResource.backgroundTertiary` - 第三背景 (#E5E5EA)
- `ColorResource.backgroundElevated` - 提升背景 (#FFFFFF)
- `ColorResource.backgroundCard` - 卡片背景 (#F8FAFC)
- `ColorResource.backgroundPage` - 页面背景 (#FAFBFC)
- `ColorResource.backgroundInput` - 输入框背景 (#F5F5F7)

### 覆盖层颜色
- `ColorResource.overlayHeavy` - 重度覆盖 (90% 不透明)
- `ColorResource.overlayStrong` - 强覆盖 (80% 不透明)
- `ColorResource.overlayMedium` - 中度覆盖 (70% 不透明)
- `ColorResource.overlayRegular` - 常规覆盖 (60% 不透明)
- `ColorResource.overlayLight` - 轻度覆盖 (50% 不透明)
- `ColorResource.overlaySubtle` - 微妙覆盖 (40% 不透明)

### 阴影颜色
- `ColorResource.shadowColorStrong` - 强阴影 (50% 不透明)
- `ColorResource.shadowColorMedium` - 中阴影 (40% 不透明)
- `ColorResource.shadowColorRegular` - 常规阴影 (30% 不透明)
- `ColorResource.shadowColorLight` - 浅阴影 (25% 不透明)
- `ColorResource.shadowColorSubtle` - 微妙阴影 (20% 不透明)
- `ColorResource.shadowColorFaint` - 淡阴影 (15% 不透明)

### 悬停效果颜色
- `ColorResource.hoverPrimary` - 主色悬停
- `ColorResource.hoverPrimaryLight` - 浅主色悬停
- `ColorResource.hoverSuccess` - 成功色悬停
- `ColorResource.hoverWarning` - 警告色悬停
- `ColorResource.hoverError` - 错误色悬停
- `ColorResource.hoverNeutral` - 中性色悬停

### 边框颜色
- `ColorResource.borderDefault` - 默认边框 (#E2E8F0)
- `ColorResource.borderGrid` - 网格线 (#E0E0E0)
- `ColorResource.borderGridFine` - 细网格线 (#F0F0F0)
- `ColorResource.borderDivider` - 分割线 (#E2E8F0)

### 文字颜色
- `ColorResource.textPrimary` - 主要文字 (#1D1D1F)
- `ColorResource.textSecondary` - 次要文字 (#666666)
- `ColorResource.textTertiary` - 第三级文字 (#86868B)
- `ColorResource.textSuccess` - 成功文字 (#28A745)
- `ColorResource.textMuted` - 柔和文字 (#64748B)
- `ColorResource.textSubdued` - 低调文字 (#94A3B8)
- `ColorResource.textStrong` - 强调文字 (#1E293B)

### 任务状态颜色
- `ColorResource.taskCritical` - 关键任务 (#D0021B)
- `ColorResource.taskNormal` - 普通任务 (#4A90E2)
- `ColorResource.taskSelected` - 已选任务 (#FF8C00)
- `ColorResource.taskProgress` - 进行中任务 (#2ECC71)

### 项目类型颜色
- `ColorResource.projectDevelopment` - 开发项目 (#3B82F6)
- `ColorResource.projectDesign` - 设计项目 (#8B5CF6)
- `ColorResource.projectMarketing` - 营销项目 (#F59E0B)
- `ColorResource.projectResearch` - 研究项目 (#10B981)
- `ColorResource.projectOther` - 其他项目 (#6B7280)

### 画布网格线颜色
- `ColorResource.canvasGridLine` - 画布网格线 (#E0E0E0)
- `ColorResource.canvasGridLineFine` - 画布细网格线 (#F0F0F0)

### 装饰性颜色（10级）
- `ColorResource.decorativeSlate` - 石板色
- `ColorResource.decorativeSlateDark` - 深石板色
- `ColorResource.decorativeStone` - 石头色
- `ColorResource.decorativePearl` - 珍珠色
- `ColorResource.decorativeFog` - 雾色
- `ColorResource.decorativeCloud` - 云色
- `ColorResource.decorativeMist` - 薄雾色
- `ColorResource.decorativeFrost` - 霜色
- `ColorResource.decorativeSnow` - 雪色
- `ColorResource.decorativeIce` - 冰色

### 特殊颜色
- `ColorResource.transparent` - 完全透明 (#00000000)

## 深色模式支持

使用 `ColorResource` 类的颜色会自动根据系统的深色模式设置切换颜色值：

- **浅色模式**：使用 `base/element/color.json` 中的颜色
- **深色模式**：使用 `dark/element/color.json` 中的颜色

无需手动判断或切换，系统会自动处理！

## 最佳实践

### ✅ 推荐做法

```typescript
// 使用语义化的颜色名称
Text('重要信息')
  .fontColor(ColorResource.textPrimary)

// 使用表面色系统创建层次感
Column() {
  // ...内容
}
.backgroundColor(ColorResource.surface)

// 使用功能色表达状态
Text('操作成功')
  .fontColor(ColorResource.successColor)
```

### ❌ 不推荐做法

```typescript
// 不要直接使用硬编码的颜色值
Text('文字')
  .fontColor('#000000')  // ❌

// 不要混用 Resource 和 string 类型
// 使用 ColorResource 时保持一致性
```

## 迁移指南

如果您的代码中已经使用了 `ThemeConfig`，可以逐步迁移到 `ColorResource`：

**迁移前：**
```typescript
import { ThemeConfig } from '../utils/ThemeConfig';

Text('标题')
  .fontColor(ThemeConfig.textPrimary)
```

**迁移后：**
```typescript
import { ColorResource } from '../utils/ThemeConfig';

Text('标题')
  .fontColor(ColorResource.textPrimary)  // 现在支持深色模式！
```

## 使用 ResponsiveColorConfig（手机端适配）

用于响应式颜色配置，支持设备类型和深色模式自动适配：

```typescript
import { ResponsiveColorConfig } from '../utils/ResponsiveColorConfig';

@Component
export struct FloatingCard {
  @Prop isDarkMode: boolean = false;
  @State screenWidth: number = 0;
  @Prop isFloating: boolean = false;

  build() {
    Column() {
      // 卡片内容
    }
    .backgroundColor(ResponsiveColorConfig.getCardBackgroundColor(
      this.isFloating,
      this.isDarkMode,
      this.screenWidth
    ))
    .borderColor(ResponsiveColorConfig.getCardBorderColor(
      this.isFloating,
      this.isDarkMode,
      this.screenWidth
    ))
    .backdropBlur(ResponsiveColorConfig.getBackdropBlur(
      this.isFloating,
      this.screenWidth
    ))
  }
}
```

### ResponsiveColorConfig 完整方法列表

#### 浮动卡片颜色
- `ResponsiveColorConfig.getFloatingCardColors(isDarkMode, screenWidth)` - 获取完整配置
- `ResponsiveColorConfig.getCardBackgroundColor(isFloating, isDarkMode, screenWidth)` - 卡片背景色
- `ResponsiveColorConfig.getCardBorderColor(isFloating, isDarkMode, screenWidth)` - 卡片边框色

#### 统计卡片颜色
- `ResponsiveColorConfig.getStatCardBackgroundColor(isFloating, isDarkMode, baseColor)` - 统计卡片背景
- `ResponsiveColorConfig.getStatCardBorderColor(isFloating, isDarkMode, baseColor)` - 统计卡片边框

#### 任务卡片颜色
- `ResponsiveColorConfig.getTaskCardBackgroundColor(isFloating, isDarkMode)` - 任务卡片背景
- `ResponsiveColorConfig.getTaskCardBorderColor(isFloating, isDarkMode)` - 任务卡片边框

#### 毛玻璃效果
- `ResponsiveColorConfig.getBackdropBlur(isFloating, screenWidth)` - 毛玻璃模糊度

#### 侧边栏颜色
- `ResponsiveColorConfig.getSidebarBackgroundColor(screenWidth)` - 侧边栏背景色
- `ResponsiveColorConfig.getSidebarBackdropBlur(screenWidth)` - 侧边栏模糊度

#### 深色模式适配
- `ResponsiveColorConfig.getDeleteButtonColor(isDarkMode)` - 删除按钮颜色
- `ResponsiveColorConfig.getCriticalPathBadgeColor(isDarkMode)` - 关键路径标签颜色
- `ResponsiveColorConfig.getFloatingPanelBackgroundColor(isDarkMode)` - 浮动面板背景色
- `ResponsiveColorConfig.getOverlayColor(isDarkMode, opacity)` - 透明遮罩颜色
- `ResponsiveColorConfig.getGridLineColor(isDarkMode, isFine)` - 网格线颜色

#### 智能颜色
- `ResponsiveColorConfig.getContrastTextColor(backgroundColor)` - 自动选择对比文字颜色

详见：[手机端颜色适配指南](手机端颜色适配指南.md)

---

## 总结

- 🎨 **110+ 颜色定义**，涵盖所有使用场景
- 🌓 **自动深色模式支持**，无需手动判断
- 📱 **响应式颜色配置**，完美适配手机端
- 📦 **三种使用方式**，灵活适应不同需求
- 🎯 **语义化命名**，代码更易读易维护

现在开始使用 `ColorResource` 和 `ResponsiveColorConfig` 让您的应用支持完整的深色模式和响应式布局吧！

## 相关文档

- [颜色管理统一说明](颜色管理统一说明.md) - 完整的颜色管理架构
- [手机端颜色适配指南](手机端颜色适配指南.md) - 手机端特殊适配详解
- [颜色系统使用指南](颜色系统使用指南.md) - 颜色命名和使用规范

