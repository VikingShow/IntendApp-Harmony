# 手机端颜色适配指南

## 📱 概述

本指南介绍 EasyFlow 项目中针对手机端和不同屏幕尺寸设备的响应式颜色管理方案。通过使用 `ResponsiveColorConfig` 工具类，您可以轻松实现在不同设备和深色/浅色模式下的颜色自动适配。

---

## 🎯 核心特性

### 1. 自动响应式适配
- 根据设备类型（手机/平板/大屏）自动选择合适的颜色
- 根据深色/浅色模式自动切换颜色
- 支持浮动卡片模式的特殊颜色处理

### 2. 毛玻璃效果优化
- 手机端使用更强的毛玻璃模糊效果
- 平板和大屏使用标准模糊效果
- 自动调整透明度和背景色

### 3. 颜色对比度优化
- 确保文字在不同背景下的可读性
- 深色模式下的颜色自动调整
- 浮动卡片的高对比度设计

---

## 📚 API 文档

### 浮动卡片颜色配置

#### `getFloatingCardColors(isDarkMode: boolean, screenWidth: number = 0)`

获取浮动卡片的完整颜色配置。

**参数：**
- `isDarkMode`: 是否为深色模式
- `screenWidth`: 屏幕宽度（vp），用于判断设备类型

**返回：**
```typescript
{
  backgroundColor: string | Resource,     // 背景颜色
  borderColor: string | Resource,         // 边框颜色
  textPrimaryColor: string | Resource,    // 主文字颜色
  textSecondaryColor: string | Resource,  // 次要文字颜色
  backdropBlur: number,                   // 毛玻璃模糊度
  opacity: number                         // 透明度
}
```

**示例：**
```typescript
import { ResponsiveColorConfig } from '../utils/ResponsiveColorConfig';

@Component
export struct MyCard {
  @State isDarkMode: boolean = false;
  @State screenWidth: number = 0;

  build() {
    const config = ResponsiveColorConfig.getFloatingCardColors(this.isDarkMode, this.screenWidth);
    
    Column() {
      // 内容
    }
    .backgroundColor(config.backgroundColor)
    .borderColor(config.borderColor)
    .backdropBlur(config.backdropBlur)
  }
}
```

---

### 卡片颜色方法

#### `getCardBackgroundColor(isFloating: boolean, isDarkMode: boolean, screenWidth: number = 0)`

获取卡片背景色（响应式）。

**使用场景：**
- 浮动卡片背景
- 对话框背景
- 弹出面板背景

**示例：**
```typescript
Column()
  .backgroundColor(ResponsiveColorConfig.getCardBackgroundColor(true, this.isDarkMode, this.screenWidth))
```

#### `getCardBorderColor(isFloating: boolean, isDarkMode: boolean, screenWidth: number = 0)`

获取卡片边框色（响应式）。

**示例：**
```typescript
Column()
  .border({
    width: 1,
    color: ResponsiveColorConfig.getCardBorderColor(true, this.isDarkMode, this.screenWidth)
  })
```

---

### 统计卡片颜色方法

#### `getStatCardBackgroundColor(isFloating: boolean, isDarkMode: boolean, baseColor?: string | Resource)`

获取统计卡片背景色（响应式）。

**适用于：**
- Dashboard 统计卡片
- 数据展示卡片
- 指标卡片

**示例：**
```typescript
Column() {
  Text('总任务数')
  Text('100')
}
.backgroundColor(ResponsiveColorConfig.getStatCardBackgroundColor(this.isFloatingCard, this.isDarkMode))
```

#### `getStatCardBorderColor(isFloating: boolean, isDarkMode: boolean, baseColor: string | Resource)`

获取统计卡片边框色（响应式）。

**示例：**
```typescript
Column()
  .border({
    width: 2,
    color: ResponsiveColorConfig.getStatCardBorderColor(
      this.isFloatingCard, 
      this.isDarkMode, 
      ColorResource.primaryLight
    )
  })
```

---

### 任务卡片颜色方法

#### `getTaskCardBackgroundColor(isFloating: boolean, isDarkMode: boolean)`

获取任务卡片背景色（响应式）。

**示例：**
```typescript
Row() {
  Text(task.name)
}
.backgroundColor(ResponsiveColorConfig.getTaskCardBackgroundColor(this.isFloatingCard, this.isDarkMode))
```

#### `getTaskCardBorderColor(isFloating: boolean, isDarkMode: boolean)`

获取任务卡片边框色（响应式）。

---

### 毛玻璃效果方法

#### `getBackdropBlur(isFloating: boolean, screenWidth: number = 0)`

获取毛玻璃模糊度（响应式）。

**返回值：**
- 手机端浮动模式：20
- 平板/大屏浮动模式：15
- 非浮动模式：0

**示例：**
```typescript
Column()
  .backdropBlur(ResponsiveColorConfig.getBackdropBlur(this.isFloatingCard, this.screenWidth))
```

---

### 侧边栏颜色方法

#### `getSidebarBackgroundColor(screenWidth: number = 0)`

获取侧边栏背景色（响应式）。

**特点：**
- 手机端：使用更强的毛玻璃效果（overlayStrong）
- 平板/大屏：使用标准毛玻璃效果（overlayMedium）

**示例：**
```typescript
Column()
  .backgroundColor(ResponsiveColorConfig.getSidebarBackgroundColor(this.screenWidth))
```

#### `getSidebarBackdropBlur(screenWidth: number = 0)`

获取侧边栏毛玻璃模糊度（响应式）。

**返回值：**
- 手机端：25
- 平板/大屏：20

---

### 深色模式适配方法

#### `getDeleteButtonColor(isDarkMode: boolean)`

获取删除按钮颜色（深色模式适配）。

**示例：**
```typescript
Button('删除')
  .fontColor(ResponsiveColorConfig.getDeleteButtonColor(this.isDarkMode))
```

#### `getCriticalPathBadgeColor(isDarkMode: boolean)`

获取关键路径标签背景色（深色模式适配）。

**示例：**
```typescript
Text('关键路径')
  .backgroundColor(ResponsiveColorConfig.getCriticalPathBadgeColor(this.isDarkMode))
```

#### `getFloatingPanelBackgroundColor(isDarkMode: boolean)`

获取浮动面板背景色（深色模式适配）。

**示例：**
```typescript
Column()
  .backgroundColor(ResponsiveColorConfig.getFloatingPanelBackgroundColor(this.isDarkMode))
```

#### `getOverlayColor(isDarkMode: boolean, opacity: number = 0.5)`

获取透明遮罩颜色（深色模式适配）。

**参数：**
- `opacity`: 透明度 (0-1)

**示例：**
```typescript
Column()
  .backgroundColor(ResponsiveColorConfig.getOverlayColor(this.isDarkMode, 0.3))
```

#### `getGridLineColor(isDarkMode: boolean, isFine: boolean = false)`

获取网格线颜色（深色模式适配）。

**参数：**
- `isFine`: 是否为细线

**示例：**
```typescript
Line()
  .stroke(ResponsiveColorConfig.getGridLineColor(this.isDarkMode, true))
```

---

### 智能颜色方法

#### `getContrastTextColor(backgroundColor: string)`

根据背景色自动选择合适的文字颜色。

**原理：**
- 计算背景色的相对亮度
- 亮度 > 0.5：返回深色文字
- 亮度 ≤ 0.5：返回浅色文字

**示例：**
```typescript
const bgColor = '#007AFF';
Text('标题')
  .fontColor(ResponsiveColorConfig.getContrastTextColor(bgColor))
```

---

## 💡 最佳实践

### 1. 浮动卡片设计

**推荐做法：**
```typescript
@Component
export struct FloatingCard {
  @Prop isFloating: boolean = false;
  @Prop isDarkMode: boolean = false;
  @State screenWidth: number = 0;

  build() {
    Column() {
      // 卡片内容
    }
    .backgroundColor(ResponsiveColorConfig.getCardBackgroundColor(
      this.isFloating, 
      this.isDarkMode, 
      this.screenWidth
    ))
    .border({
      width: 1,
      color: ResponsiveColorConfig.getCardBorderColor(
        this.isFloating, 
        this.isDarkMode, 
        this.screenWidth
      )
    })
    .backdropBlur(ResponsiveColorConfig.getBackdropBlur(
      this.isFloating, 
      this.screenWidth
    ))
  }
}
```

### 2. 响应式统计卡片

**推荐做法：**
```typescript
@Builder
StatCard(title: string, value: string, bgColor: Resource) {
  Column() {
    Text(title)
      .fontColor(ColorResource.textSecondary)
    Text(value)
      .fontColor(bgColor)
  }
  .backgroundColor(ResponsiveColorConfig.getStatCardBackgroundColor(
    this.isFloatingCard, 
    this.isDarkMode
  ))
  .border({
    width: this.isFloatingCard ? 1 : 2,
    color: ResponsiveColorConfig.getStatCardBorderColor(
      this.isFloatingCard,
      this.isDarkMode,
      bgColor
    )
  })
}
```

### 3. 深色模式按钮

**推荐做法：**
```typescript
Button('删除')
  .fontColor(ResponsiveColorConfig.getDeleteButtonColor(this.isDarkMode))
  .backgroundColor(Color.Transparent)
```

### 4. 侧边栏设计

**推荐做法：**
```typescript
Column()
  .backgroundColor(ResponsiveColorConfig.getSidebarBackgroundColor(this.screenWidth))
  .backdropBlur(ResponsiveColorConfig.getSidebarBackdropBlur(this.screenWidth))
  .border({ width: { right: 1 }, color: ColorResource.surfaceNeutral })
```

---

## 🔍 常见问题

### Q1: 什么时候使用 ResponsiveColorConfig？

**答：** 在以下场景使用：
- 浮动卡片/对话框
- 需要深色模式适配的组件
- 需要响应式颜色的布局
- 手机端特殊的颜色需求

### Q2: 什么时候使用 ColorResource？

**答：** 在以下场景使用：
- 标准UI组件（按钮、文本等）
- 固定颜色的元素
- 不需要特殊响应式处理的场景

### Q3: 如何获取屏幕宽度？

**答：** 在组件中添加：
```typescript
@State private screenWidth: number = 0;

aboutToAppear() {
  this.updateScreenSize();
}

private updateScreenSize() {
  try {
    const uiContext = this.getUIContext();
    // 实际获取屏幕宽度的代码
    this.screenWidth = 840; // 默认值
  } catch (error) {
    this.screenWidth = 840;
  }
}
```

### Q4: 如何判断深色模式？

**答：** 使用系统配置监听：
```typescript
@State isDarkMode: boolean = false;

aboutToAppear() {
  // 获取系统配置
  const configuration = this.context.config.colorMode;
  this.isDarkMode = configuration === ConfigurationConstant.ColorMode.COLOR_MODE_DARK;
}
```

---

## 📋 颜色对照表

### 手机端 vs 平板/大屏

| 场景 | 手机端 | 平板/大屏 |
|-----|--------|----------|
| 浮动卡片背景透明度 | 0.85 | 0.7 |
| 浮动卡片边框透明度 | 0.7 | 0.6 |
| 毛玻璃模糊度 | 20 | 15 |
| 侧边栏毛玻璃 | 25 | 20 |
| 侧边栏背景 | overlayStrong | overlayMedium |

### 深色模式 vs 浅色模式

| 元素 | 深色模式 | 浅色模式 |
|-----|---------|----------|
| 删除按钮 | #FF6B6B | #FF453A |
| 关键路径标签 | rgba(255,107,107,0.12) | rgba(255,69,58,0.08) |
| 浮动面板背景 | rgba(28,28,30,0.95) | rgba(249,249,251,0.95) |
| 网格线（粗） | rgba(255,255,255,0.06) | rgba(0,0,0,0.06) |
| 网格线（细） | rgba(255,255,255,0.03) | rgba(0,0,0,0.03) |

---

## 🎨 设计理念

### 1. 渐进增强
- 基础功能使用标准 ColorResource
- 特殊场景使用 ResponsiveColorConfig
- 确保向后兼容性

### 2. 响应式优先
- 所有颜色都考虑设备类型
- 自动适配屏幕尺寸
- 优化手机端体验

### 3. 深色模式友好
- 所有颜色都支持深色模式
- 自动调整对比度
- 保持视觉一致性

### 4. 性能优化
- 颜色计算轻量级
- 避免重复计算
- 使用缓存策略

---

## 📖 相关文档

- [颜色系统使用指南](颜色系统使用指南.md) - 完整的颜色系统文档
- [颜色资源使用指南](颜色资源使用指南.md) - ColorResource 使用指南
- [BreakpointSystem](entry/src/main/ets/utils/BreakpointSystem.ets) - 断点系统文档
- [ResponsiveColorConfig](entry/src/main/ets/utils/ResponsiveColorConfig.ets) - 源代码

---

## 🚀 快速开始

### 步骤 1: 导入工具类

```typescript
import { ResponsiveColorConfig } from '../utils/ResponsiveColorConfig';
import { ColorResource } from '../utils/ThemeConfig';
import { BreakpointSystem } from '../utils/BreakpointSystem';
```

### 步骤 2: 添加状态变量

```typescript
@State private screenWidth: number = 0;
@State private isDarkMode: boolean = false;
@Prop isFloatingCard: boolean = false;
```

### 步骤 3: 使用响应式颜色

```typescript
Column() {
  // 内容
}
.backgroundColor(ResponsiveColorConfig.getCardBackgroundColor(
  this.isFloatingCard,
  this.isDarkMode,
  this.screenWidth
))
```

---

*最后更新：2025年10月*

