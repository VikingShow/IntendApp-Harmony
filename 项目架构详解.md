# EasyFlow 项目架构详解

## 项目概述

EasyFlow 是一个基于 HarmonyOS ArkTS 开发的项目管理应用，采用关键路径法（CPM）进行项目规划和调度。项目采用分层架构设计，实现了低耦合、高内聚的代码结构。

## 目录结构

```
entry/src/main/ets/
├── core/           # 核心业务逻辑层
├── components/     # UI组件层
├── pages/         # 页面层
├── services/      # 服务层
├── model/         # 数据模型层
├── utils/         # 工具类层
├── entryability/  # 应用入口层
└── entrybackupability/  # 备份能力层
```

## 分层架构详解

### 1. 数据模型层 (Model Layer)

#### 📁 `model/ProjectData.ets`
**作用**: 定义项目的核心数据结构和接口

**核心内容**:
- `ChecklistItem`: 任务检查项接口
- `TaskNode`: 任务节点接口（包含时间、位置、关键路径信息）
- `Dependency`: 任务依赖关系接口
- `Project`: 项目类（@Observed装饰器，支持响应式更新）

**依赖关系**: 
- 被所有其他层引用
- 是数据流的基础

### 2. 核心业务逻辑层 (Core Layer)

#### 📁 `core/ProjectManager.ets`
**作用**: 项目管理的核心控制器，实现单例模式

**核心功能**:
- 项目状态管理（加载、保存、创建、更新）
- 任务管理（增删改查、依赖关系管理）
- 关键路径计算
- 事件驱动状态更新

**依赖关系**:
- 依赖: `ProjectData`, `StorageService`, `CriticalPathService`, `EventBus`
- 被依赖: `pages/Index.ets`, `pages/Home.ets`

#### 📁 `core/EventBus.ets`
**作用**: 事件总线，实现组件间解耦通信

**核心功能**:
- 事件订阅/发布机制
- 类型安全的事件参数定义
- 全局事件管理

**依赖关系**:
- 依赖: 无
- 被依赖: `ProjectManager`, `pages/Index.ets`

#### 📁 `core/CanvasRenderer.ets`
**作用**: 画布渲染器，负责绘制项目图表

**核心功能**:
- 任务节点绘制
- 依赖关系连线绘制
- 关键路径高亮显示
- 画布状态管理

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `CanvasController`

#### 📁 `core/CanvasController.ets`
**作用**: 画布控制器，处理用户交互

**核心功能**:
- 触摸事件处理
- 拖拽操作管理
- 连接模式管理
- 画布锁定/解锁

**依赖关系**:
- 依赖: `CanvasRenderer`, `ProjectData`
- 被依赖: `pages/Index.ets`

### 3. 服务层 (Service Layer)

#### 📁 `services/StorageService.ets`
**作用**: 数据持久化服务

**核心功能**:
- 项目数据本地存储
- 数据序列化/反序列化
- 文件系统操作

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `ProjectManager`

#### 📁 `services/CriticalPathService.ets`
**作用**: 关键路径计算服务

**核心功能**:
- CPM算法实现
- 最早/最晚时间计算
- 关键路径识别

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `ProjectManager`

#### 📁 `services/TemplateService.ets`
**作用**: 项目模板服务

**核心功能**:
- 预定义项目模板管理
- 模板数据提供
- 模板创建项目

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `components/TemplateDialog.ets`

#### 📁 `services/StatisticsService.ets`
**作用**: 统计分析服务

**核心功能**:
- 项目进度统计
- 任务完成率计算
- 时间分析

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `components/StatisticsView.ets`

#### 📁 `services/SearchService.ets`
**作用**: 搜索服务

**核心功能**:
- 任务搜索
- 项目搜索
- 搜索结果排序

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `components/SearchView.ets`

#### 📁 `services/ExportService.ets`
**作用**: 数据导出服务

**核心功能**:
- 项目数据导出
- 多种格式支持
- 文件生成

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `components/ExportDialog.ets`

#### 📁 `services/LoadingService.ets`
**作用**: 加载状态管理服务

**核心功能**:
- 加载状态控制
- 进度显示

**依赖关系**:
- 依赖: 无
- 被依赖: `components/LoadingView.ets`

### 4. UI组件层 (Component Layer)

#### 📁 `components/Sidebar.ets`
**作用**: 主侧边栏组件

**核心功能**:
- 项目信息显示
- 任务编辑器切换
- 操作按钮管理

**依赖关系**:
- 依赖: `ProjectData`, `TaskEditor`, `WelcomeView`, `TemplateDialog`
- 被依赖: `pages/Index.ets`

#### 📁 `components/TaskEditor.ets`
**作用**: 任务编辑器组件

**核心功能**:
- 任务详情编辑
- 检查项管理
- 时间设置

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `Sidebar.ets`

#### 📁 `components/WelcomeView.ets`
**作用**: 欢迎页面组件

**核心功能**:
- 新建项目对话框
- 模板选择入口

**依赖关系**:
- 依赖: 无
- 被依赖: `Sidebar.ets`

#### 📁 `components/TemplateDialog.ets`
**作用**: 模板选择对话框

**核心功能**:
- 模板列表显示
- 模板预览
- 模板选择

**依赖关系**:
- 依赖: `TemplateService`
- 被依赖: `Sidebar.ets`

#### 📁 `components/StatisticsView.ets`
**作用**: 统计视图组件

**核心功能**:
- 项目统计图表
- 进度可视化
- 数据分析展示

**依赖关系**:
- 依赖: `StatisticsService`
- 被依赖: `pages/Home.ets`

#### 📁 `components/SearchView.ets`
**作用**: 搜索视图组件

**核心功能**:
- 搜索界面
- 搜索结果展示
- 搜索过滤

**依赖关系**:
- 依赖: `SearchService`
- 被依赖: `pages/Home.ets`

#### 📁 `components/ExportDialog.ets`
**作用**: 导出对话框组件

**核心功能**:
- 导出格式选择
- 导出选项配置
- 导出进度显示

**依赖关系**:
- 依赖: `ExportService`
- 被依赖: `pages/Home.ets`

#### 📁 `components/DashboardView.ets`
**作用**: 仪表板视图组件

**核心功能**:
- 项目概览
- 快速操作
- 状态展示

**依赖关系**:
- 依赖: `ProjectData`
- 被依赖: `pages/Home.ets`

#### 📁 `components/LoadingView.ets`
**作用**: 加载视图组件

**核心功能**:
- 加载动画
- 进度显示
- 状态提示

**依赖关系**:
- 依赖: `LoadingService`
- 被依赖: 多个页面和组件

#### 📁 `components/CommonButton.ets`
**作用**: 通用按钮组件

**核心功能**:
- 统一按钮样式
- 可配置属性
- 事件处理

**依赖关系**:
- 依赖: 无
- 被依赖: 多个组件

### 5. 页面层 (Page Layer)

#### 📁 `pages/Index.ets`
**作用**: 项目编辑页面

**核心功能**:
- 项目编辑界面
- 画布和侧边栏布局
- 事件监听和状态同步

**依赖关系**:
- 依赖: `ProjectManager`, `CanvasController`, `Sidebar`, `ProjectData`, `EventBus`
- 被依赖: `entryability/EntryAbility.ets`

#### 📁 `pages/Home.ets`
**作用**: 项目主页

**核心功能**:
- 项目列表管理
- 新建项目入口
- 项目操作界面

**依赖关系**:
- 依赖: `ProjectManager`, `StorageService`, 多个组件
- 被依赖: `entryability/EntryAbility.ets`

### 6. 工具类层 (Utils Layer)

#### 📁 `utils/ThemeConfig.ets`
**作用**: 主题配置工具

**核心功能**:
- 颜色主题定义
- 样式配置
- 主题切换

**依赖关系**:
- 依赖: 无
- 被依赖: 多个组件

#### 📁 `utils/ValidationUtils.ets`
**作用**: 数据验证工具

**核心功能**:
- 输入验证
- 数据格式检查
- 错误提示

**依赖关系**:
- 依赖: 无
- 被依赖: 多个组件和服务

### 7. 应用入口层 (Entry Layer)

#### 📁 `entryability/EntryAbility.ets`
**作用**: 应用入口能力

**核心功能**:
- 应用生命周期管理
- 页面路由
- 权限管理

**依赖关系**:
- 依赖: `pages/Home.ets`, `pages/Index.ets`
- 被依赖: HarmonyOS系统

## 数据流向图

```
用户操作 → 页面层 → 核心层 → 服务层 → 数据层
    ↑                                    ↓
    ←────────── 事件总线 ←───────────────←
```

## 架构优势

### 1. 分层清晰
- **数据模型层**: 统一的数据结构定义
- **核心层**: 业务逻辑集中管理
- **服务层**: 功能模块化，职责单一
- **组件层**: UI组件可复用
- **页面层**: 页面逻辑简单清晰

### 2. 低耦合设计
- **事件驱动**: 通过EventBus实现组件间解耦
- **依赖注入**: 通过服务层提供功能
- **接口分离**: 明确的接口定义

### 3. 高内聚实现
- **单一职责**: 每个模块职责明确
- **功能封装**: 相关功能集中管理
- **状态管理**: 统一的状态管理机制

### 4. 可扩展性
- **模块化设计**: 易于添加新功能
- **插件化架构**: 服务可独立扩展
- **配置化**: 主题和配置可灵活调整

## 关键技术特性

### 1. ArkTS响应式编程
- 使用`@State`、`@Prop`、`@ObjectLink`装饰器
- 实现UI与数据的自动同步

### 2. 事件驱动架构
- EventBus实现组件间通信
- 避免直接依赖，提高可维护性

### 3. 单例模式
- ProjectManager和EventBus使用单例
- 确保全局状态一致性

### 4. 关键路径算法
- 实现CPM算法进行项目调度
- 自动计算关键路径和浮动时间

## 总结

EasyFlow项目采用了现代化的分层架构设计，通过清晰的职责分离和事件驱动机制，实现了高内聚、低耦合的代码结构。这种架构不仅提高了代码的可维护性和可扩展性，也为后续功能迭代奠定了坚实的基础。

