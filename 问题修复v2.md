# 🔧 手机端Canvas渲染问题修复 v2

## 📋 本次修复内容

### 🎯 修复1：彻底解决节点闪烁问题

**问题根源：**
虽然在 `aboutToAppear` 中已经通过 `determineDeviceType()` 确定了设备类型，但在 `Canvas.onReady()` 中还有一段**二次设置渲染模式**的逻辑：

```typescript
// ❌ 旧代码 - 会导致闪烁
const correctRenderMode = this.isPhone ? RenderMode.COMPACT : RenderMode.NORMAL;
this.canvasRenderMode = correctRenderMode;
this.canvasController.setRenderMode(correctRenderMode);
```

**问题流程：**
```
1. aboutToAppear() 开始执行
2. ✅ determineDeviceType() 完成，isPhone = true
3. ✅ 设置 canvasRenderMode = COMPACT
4. Canvas 组件开始渲染
5. ❌ Canvas.onReady() 触发时，this.isPhone 可能还是 false（@State 更新延迟）
6. ❌ 二次设置为 NORMAL 模式
7. ❌ drawGraph() 画出 200x120 大节点 💥
8. ✅ @State 更新完成，this.isPhone = true
9. ✅ 再次渲染，切换到 COMPACT 模式（圆圈）
10. 💥 用户看到：节点突然变大再缩小
```

**修复方案：**
移除 `Canvas.onReady()` 中的二次设置逻辑，直接使用 `aboutToAppear` 中已确定的模式：

```typescript:entry/src/main/ets/pages/Index.ets
// ✅ 新代码 - Index.ets Canvas.onReady()
.onReady(() => {
  console.log(`[Index] Canvas onReady - Using pre-determined renderMode: ${this.canvasRenderMode}`);
  
  if (this.project) {
    // ✅ 不再二次设置渲染模式，使用预先确定的模式
    this.restoreCanvasPosition();
    this.canvasController.getRenderer().clearRenderCache();
    this.drawGraph();
    this.canvasOpacity = 1;
  }
})
```

**预期效果：**
- ✅ 进入编辑页时，Canvas 直接使用 `COMPACT` 模式
- ✅ 节点不再闪烁变大
- ✅ 渲染模式在整个生命周期保持一致

---

### 🎯 修复2：确保每次返回都能静默更新

**问题根源：**
1. 条件判断太严格：`if (this.isPhone && this.phoneViewMode === 'list')`
2. `phoneViewMode` 可能在某些情况下不是 `'list'`
3. 延迟时间不够，`selectedProjectDetail` 可能还没加载完成

**修复方案：**

#### 1. 放宽条件判断
```typescript:entry/src/main/ets/pages/Home.ets
// ❌ 旧代码
if (this.isPhone && this.phoneViewMode === 'list') {
  // 自动显示Dashboard
}

// ✅ 新代码 - 只要是手机端就自动显示
if (this.isPhone) {
  console.log(`[Home] onPageShow - Auto-showing Dashboard card for phone`);
  setTimeout(() => {
    if (this.selectedProjectDetail) {
      this.showCanvasBackground = true;
      this.showDashboardCard = true;
      this.canvasController.getRenderer().clearRenderCache();
      this.renderProjectCanvas();
    }
  }, 300); // 增加延迟到300ms
}
```

#### 2. 优化 silentRefreshProject
```typescript:entry/src/main/ets/pages/Home.ets
async silentRefreshProject(projectId: string) {
  // ...加载项目数据
  
  if (loadedProject) {
    this.selectedProjectDetail = loadedProject;
    
    console.log(`[Home] ✅ Project data silently updated: ${loadedProject.name}, Tasks: ${loadedProject.tasks.length}`);
    
    // ✅ 平板端：立即渲染（Canvas已挂载）
    if (!this.isPhone) {
      this.renderProjectCanvas();
    }
    // ✅ 手机端：等待 onPageShow 显示 Dashboard 时自动渲染
  }
}
```

#### 3. 添加详细日志
```typescript
onPageShow() {
  console.log(`[Home] onPageShow - isPhone: ${this.isPhone}, phoneViewMode: ${this.phoneViewMode}, selectedProjectId: ${this.selectedProjectId}`);
  
  // ...
  
  setTimeout(() => {
    console.log(`[Home] onPageShow timeout - selectedProjectDetail exists: ${!!this.selectedProjectDetail}`);
    if (this.selectedProjectDetail) {
      console.log(`[Home] onPageShow - Setting showDashboardCard=true`);
      // 显示Dashboard
    } else {
      console.warn(`[Home] onPageShow - selectedProjectDetail is null, cannot show dashboard`);
    }
  }, 300);
}
```

---

## 🧪 测试步骤

### 测试1：节点闪烁问题

1. **准备环境**
   - 使用手机设备或手机模拟器
   - 确保项目列表中至少有一个项目

2. **测试步骤**
   ```
   步骤1: 在项目列表中点击任意项目
   步骤2: 等待Dashboard卡片显示
   步骤3: 点击右上角"编辑"按钮进入编辑页
   
   ✅ 预期：节点应该直接以圆圈形式显示，不应该先变大再缩小
   ❌ 失败：如果看到节点先显示为矩形再变成圆圈
   ```

3. **查看日志**
   ```
   关键日志应该是：
   [Index] Device type determined: screenWidth=360vp, isPhone=true
   [Index] aboutToAppear - Device determined: ..., renderMode=COMPACT
   [Index] Canvas onReady - Using pre-determined renderMode: COMPACT
   [Index] Canvas now visible with opacity: 1
   
   ❌ 如果看到两次设置渲染模式，说明还有问题
   ```

---

### 测试2：Dashboard静默更新

1. **第一次测试**
   ```
   步骤1: 点击项目进入编辑页
   步骤2: 移动几个节点
   步骤3: 点击返回按钮
   
   ✅ 预期：自动显示Dashboard卡片，背景显示最新节点位置
   ❌ 失败：Dashboard未显示或显示旧状态
   ```

2. **第二次测试（关键）**
   ```
   步骤1: 关闭Dashboard卡片（点击X按钮）
   步骤2: 再次点击项目进入编辑页
   步骤3: 再次移动节点
   步骤4: 点击返回按钮
   
   ✅ 预期：Dashboard应该再次自动显示，且显示最新状态
   ❌ 失败：Dashboard未显示或显示旧状态
   ```

3. **第三次测试（多次往返）**
   ```
   重复上述步骤3-5次
   
   ✅ 预期：每次都能自动显示最新状态
   ❌ 失败：某次未自动显示
   ```

4. **查看日志**
   ```
   关键日志应该包含：
   [Home] onPageShow - isPhone: true, phoneViewMode: list, selectedProjectId: xxx
   [Home] Silently refreshing project: xxx
   [Home] ✅ Project data silently updated: 项目名, Tasks: 5, ...
   [Home] onPageShow - Auto-showing Dashboard card for phone (showDashboardCard: false)
   [Home] onPageShow timeout - selectedProjectDetail exists: true
   [Home] onPageShow - Setting showDashboardCard=true, showCanvasBackground=true
   [Home] PhoneDashboardCard Canvas onReady - rendering with latest data
   
   ❌ 如果缺少上述日志，或者 selectedProjectDetail 为 null，需要调试
   ```

---

## 🔍 如果问题还存在

### 问题1：节点还是闪烁

**可能原因：**
1. `determineDeviceType()` 没有正确执行
2. `@State isPhone` 更新延迟
3. Canvas 组件在 `aboutToAppear` 之前就创建了

**调试步骤：**
```typescript
// 在 determineDeviceType() 中添加
console.log(`[Index] determineDeviceType START`);
// ...
console.log(`[Index] determineDeviceType END - isPhone: ${this.isPhone}`);

// 在 aboutToAppear() 中添加
console.log(`[Index] aboutToAppear START - isPhone: ${this.isPhone}`);
await this.determineDeviceType();
console.log(`[Index] aboutToAppear AFTER determineDeviceType - isPhone: ${this.isPhone}`);
```

**如果日志显示 `isPhone` 一直是 `false`：**
- 检查 `BreakpointSystem.isPhone()` 的逻辑
- 手动设置断点值（临时方案）：
  ```typescript
  this.isPhone = true; // 强制手机模式
  ```

---

### 问题2：第二次返回不更新

**可能原因：**
1. `selectedProjectDetail` 在第二次时为 `null`
2. `showDashboardCard` 状态混乱
3. Canvas 没有重新挂载

**调试步骤：**
```typescript
// 在 onPageShow() 的 setTimeout 中添加
setTimeout(() => {
  console.log(`[Debug] selectedProjectDetail: ${JSON.stringify(this.selectedProjectDetail?.name)}`);
  console.log(`[Debug] showDashboardCard: ${this.showDashboardCard}`);
  console.log(`[Debug] showCanvasBackground: ${this.showCanvasBackground}`);
  
  if (this.selectedProjectDetail) {
    console.log(`[Debug] Setting flags to true`);
    this.showCanvasBackground = true;
    this.showDashboardCard = true;
    
    // 强制更新
    this.componentKey++;
    
    this.canvasController.getRenderer().clearRenderCache();
    this.renderProjectCanvas();
  }
}, 300);
```

**如果 `selectedProjectDetail` 为 `null`：**
- 增加延迟时间到 500ms
- 或者在 `silentRefreshProject` 完成后再显示Dashboard：
  ```typescript
  this.silentRefreshProject(this.selectedProjectId).then(() => {
    if (this.isPhone) {
      this.showCanvasBackground = true;
      this.showDashboardCard = true;
    }
  });
  ```

---

## 📊 修复对比总结

| 问题 | 修复前 | 修复后 |
|------|--------|--------|
| **节点闪烁** | Canvas.onReady() 二次设置模式 → 闪烁 | 使用预先确定的模式 → 无闪烁 |
| **第二次不更新** | 条件太严格 + 延迟不够 | 放宽条件 + 增加延迟 + 详细日志 |
| **日志可见性** | 简单日志 | 详细日志，便于调试 |

---

## 🎉 修复要点

1. **设备类型确定时机**：在 `aboutToAppear` **最开始**同步确定
2. **渲染模式一致性**：整个生命周期使用同一个渲染模式，不二次设置
3. **Dashboard显示条件**：手机端只要有选中项目，返回时就自动显示
4. **异步延迟处理**：给数据加载留足够的时间（300ms）
5. **平板/手机分离**：`silentRefreshProject` 中区分平板和手机端的处理逻辑

如果按照这些要点修复后问题还存在，请提供控制台日志，我会进一步分析！


