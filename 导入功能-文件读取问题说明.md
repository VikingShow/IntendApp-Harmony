# 导入功能 - 文件读取问题说明

## 问题描述

在测试导入功能时，遇到文件读取失败的问题：

```
[ImportService] 已选择文件: file://docs/storage/Users/currentUser/Documents/...csv
[ImportService] 读取文件失败: Error: No such file or directory
```

## 错误日志分析

```
10-13 09:50:18.364   [ImportService] 已选择文件: file://docs/storage/Users/currentUser/Documents/%E7%BD%91%E7%AB%99%E8%AE%BE%E8%AE%A1%E9%A1%B9%E7%9B%AE_%E4%BB%BB%E5%8A%A1%E5%88%97%E8%A1%A8_2025-10-10T14-41-01.csv
10-13 09:50:18.364   [GetRealPath:156] GetRealPath  uri is ,<private>
10-13 09:50:18.364   [GetRealPath:161] GetRealPath return path is ,<private>
10-13 09:50:18.365   [ImportService] 读取文件失败: Error: No such file or directory
```

## 可能的原因

### 1. 文件路径权限问题

从日志中的 `GetRealPath return path is ,<private>` 可以看出：
- 系统尝试将 URI 转换为真实路径
- 但返回的是 `<private>`，表示路径被隐私保护
- 这可能意味着应用没有足够的权限访问该路径

### 2. URI 格式问题

- 文件选择器返回的是 `file://docs/...` 格式的 URI
- URI 中包含 URL 编码的中文字符（`%E7%BD%91...`）
- 某些系统版本可能不支持直接使用这种 URI 打开文件

### 3. 文件位置问题

文件位于：`/storage/Users/currentUser/Documents/`
- 这可能是用户私有目录
- 某些 HarmonyOS 版本可能限制应用访问用户文档目录

## 已实施的修复

### 1. 添加详细日志

```typescript
console.log(`[ImportService] 开始读取文件: ${fileUri}`);
console.log('[ImportService] 检查文件访问权限...');
console.log('[ImportService] 正在打开文件...');
console.log(`[ImportService] 文件已打开，文件描述符: ${file.fd}`);
console.log(`[ImportService] 文件大小: ${stat.size} 字节`);
```

### 2. 添加访问权限检查

```typescript
try {
  const accessible = fs.accessSync(fileUri);
  console.log(`[ImportService] 文件访问检查结果: ${accessible}`);
} catch (accessError) {
  console.error('[ImportService] 文件访问检查失败:', accessError);
  return null;
}
```

### 3. 改进错误提示

```typescript
error: '读取文件失败。请确保文件存在且应用有访问权限。\n提示：某些系统文件夹可能无法访问，请将文件复制到 Documents 或 Downloads 文件夹后重试。'
```

## 建议的解决方案

### 方案 1：使用应用沙盒目录（推荐）

1. **引导用户将文件复制到应用沙盒**
   - 在导入对话框中添加说明
   - 提示用户先将文件保存到 Downloads 或 Documents
   
2. **修改文件选择器配置**
   ```typescript
   const documentSelectOptions = new picker.DocumentSelectOptions();
   // 可能需要指定特定的起始路径
   ```

### 方案 2：申请文件访问权限

1. **在 module.json5 中添加权限**
   ```json
   {
     "requestPermissions": [
       {
         "name": "ohos.permission.READ_MEDIA",
         "reason": "$string:permission_read_media",
         "usedScene": {
           "abilities": ["EntryAbility"],
           "when": "inuse"
         }
       }
     ]
   }
   ```

2. **动态申请权限**
   ```typescript
   await abilityAccessCtrl.requestPermissionsFromUser(context, [
     'ohos.permission.READ_MEDIA'
   ]);
   ```

### 方案 3：使用 FileUri API

某些情况下，可能需要使用特殊的 API 处理 URI：

```typescript
import { fileUri } from '@kit.CoreFileKit';

// 可能需要转换 URI 格式
const realPath = fileUri.getUriFromPath(fileUri);
```

## 测试步骤

### 重新测试（获取更多诊断信息）

1. **重新运行应用**
2. **选择导入文件**
3. **查看新的日志输出**
   - 文件访问检查结果
   - 更详细的错误信息
   - 错误代码

### 预期的新日志

```
[ImportService] 开始读取文件: file://...
[ImportService] 检查文件访问权限...
[ImportService] 文件访问检查结果: true/false
[ImportService] 正在打开文件...
```

如果成功：
```
[ImportService] 文件已打开，文件描述符: 3
[ImportService] 文件大小: 1234 字节
[ImportService] 正在读取文件内容...
[ImportService] 文件内容读取完成
[ImportService] 文件内容解码完成，长度: 1234 字符
```

如果失败，会看到：
```
[ImportService] 文件访问检查失败: ...
[ImportService] 访问错误代码: ENOENT
```

## 临时解决方案

在找到根本原因之前，用户可以：

### 选项 1：使用不同的文件位置

1. 将 CSV/JSON 文件保存到 **Downloads** 文件夹
2. 或保存到 **应用的私有目录**（如果可能）

### 选项 2：使用导出功能生成文件

1. 先在应用内创建示例项目
2. 使用导出功能导出 JSON/CSV
3. 然后测试导入这些文件
4. 这样可以确保文件在可访问的位置

### 选项 3：测试简单文件

创建一个简单的测试文件：

**test.csv**:
```csv
任务ID,任务名称,开始日期,结束日期,持续天数,是否关键路径,完成进度,子任务总数,已完成子任务,X坐标,Y坐标
1,测试任务,2025/1/1,2025/1/5,5,是,0%,0,0,100,100
```

将此文件保存到 **Downloads** 目录，然后尝试导入。

## 后续行动

1. ✅ **已完成**：添加详细日志和访问检查
2. ⏳ **待测试**：重新运行应用，查看新日志
3. ⏳ **待确认**：根据新日志确定具体原因
4. ⏳ **待实施**：实施针对性的解决方案

## 相关文档

- HarmonyOS 文件管理：https://developer.harmonyos.com/cn/docs/documentation/doc-guides-V3/fileio-guidelines-0000001478061961-V3
- 文件选择器：https://developer.harmonyos.com/cn/docs/documentation/doc-references-V3/js-apis-file-picker-0000001544584301-V3
- 权限管理：https://developer.harmonyos.com/cn/docs/documentation/doc-guides-V3/accesstoken-guidelines-0000001493744069-V3

## 更新记录

- **2025-10-13**: 初始问题发现
- **2025-10-13**: 添加详细日志和访问检查
- **待更新**: 根据新测试结果更新

---

**状态**: 🔍 正在诊断  
**优先级**: 高  
**负责人**: 开发团队

