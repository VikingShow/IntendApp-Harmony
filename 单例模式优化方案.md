# CanvasController 单例模式优化方案

## 问题回顾

### 原始问题
手机端从 Home 页面点击编辑按钮进入 Index 页面时，节点会**闪烁**（先变大再恢复）。

### 问题根源
1. **多实例问题**：Home 和 Index 各自创建独立的 `CanvasController` 实例
2. **状态不同步**：两个实例的渲染模式、缩放、偏移等状态独立维护
3. **页面切换时**：从 Home（COMPACT 模式）跳转到 Index，Index 的实例需要重新初始化
4. **初始化延迟**：即使代码提前设置模式，Canvas 仍可能短暂使用默认 NORMAL 模式
5. **动画闪烁**：延迟显示虽然能掩盖问题，但治标不治本

## 解决方案：单例模式

### 核心思路
**让 Home 和 Index 共享同一个 CanvasController 实例**，状态完全同步，从根本上消除闪烁。

### 实现步骤

#### 1. 改造 CanvasController 为单例模式

**文件**：`entry/src/main/ets/core/canvas/CanvasController.ets`

**关键修改**：
```typescript
export class CanvasController {
  // ✨ 添加静态实例
  private static instance: CanvasController | null = null;
  
  // ✨ 构造函数改为私有
  private constructor() {
    this.renderer = new CanvasRenderer();
    console.log('[CanvasController] 🎨 Single instance created');
  }

  /**
   * ✨ 添加 getInstance 静态方法
   */
  public static getInstance(): CanvasController {
    if (!CanvasController.instance) {
      CanvasController.instance = new CanvasController();
      console.log('[CanvasController] ✅ First getInstance call - instance created');
    } else {
      console.log('[CanvasController] ♻️ getInstance - reusing existing instance');
    }
    return CanvasController.instance;
  }
}
```

**修改位置**：
- 第 148-149 行：添加静态实例变量
- 第 198-202 行：构造函数改为私有
- 第 204-216 行：添加 getInstance 方法

#### 2. 更新 Home.ets 使用单例

**文件**：`entry/src/main/ets/pages/Home.ets`

**修改前**：
```typescript
private canvasController: CanvasController = new CanvasController();
```

**修改后**：
```typescript
// ✨ 画布控制器：使用单例模式，与 Index 页面共享状态
private canvasController: CanvasController = CanvasController.getInstance();
```

**修改位置**：第 229 行

#### 3. 更新 Index.ets 使用单例

**文件**：`entry/src/main/ets/pages/Index.ets`

**修改前**：
```typescript
private canvasController: CanvasController = new CanvasController();
```

**修改后**：
```typescript
// ✨ 画布控制器：使用单例模式，与 Home 页面共享状态
private canvasController: CanvasController = CanvasController.getInstance();
```

**修改位置**：第 83 行

#### 4. 简化 Index.ets 初始化逻辑

**aboutToAppear 优化**（第 131-167 行）：
```typescript
// ✨ 单例模式优化：由于 CanvasController 是单例，从 Home 页面跳转过来时，
// 渲染模式已经在 Home 中设置好了，这里只需要确认即可

// 检查并同步渲染模式（通常已经由 Home 设置好）
const currentRenderMode = this.canvasController.getState().renderMode;
const expectedRenderMode = this.isPhone ? RenderMode.COMPACT : RenderMode.NORMAL;

if (currentRenderMode !== expectedRenderMode) {
  console.log(`[Index] 🔄 Syncing render mode: ${currentRenderMode} -> ${expectedRenderMode}`);
  this.canvasRenderMode = expectedRenderMode;
  this.canvasController.setRenderMode(expectedRenderMode);
} else {
  console.log(`[Index] ✅ Render mode already correct: ${currentRenderMode === RenderMode.COMPACT ? 'COMPACT' : 'NORMAL'} (from singleton)`);
  this.canvasRenderMode = currentRenderMode;
}
```

**Canvas.onReady 优化**（第 1007-1029 行）：
```typescript
.onReady(() => {
  console.log(`[Index] 📺 Canvas onReady - renderMode: ${this.canvasRenderMode === RenderMode.COMPACT ? 'COMPACT' : 'NORMAL'} (singleton)`);
  
  // ✨ 单例模式优化：渲染模式已经由单例确保一致，可以直接渲染
  if (this.project) {
    this.restoreCanvasPosition();
    this.canvasController.getRenderer().clearRenderCache();
    this.drawGraph();
    
    // ✨ 由于单例模式确保状态一致，可以立即显示，无闪烁
    this.canvasOpacity = 1;
    console.log(`[Index] ✅ Canvas visible (no delay needed with singleton)`);
  }
})
```

**project_loaded 事件优化**（第 365-380 行）：
```typescript
eventBus.on('project_loaded', () => {
  // ✨ 单例模式：渲染模式已由单例确保一致
  console.log(`[Index] 📦 project_loaded - renderMode: ${this.canvasController.getState().renderMode === RenderMode.COMPACT ? 'COMPACT' : 'NORMAL'} (singleton)`);
  
  this.canvasController.getRenderer().clearRenderCache();
  this.drawGraph();
  
  // ✨ 由于单例模式确保状态一致，可以立即显示
  this.canvasOpacity = 1;
  console.log(`[Index] ✅ Canvas visible after project_loaded (no delay with singleton)`);
});
```

## 单例模式的优势

### 1. 状态完全同步 ✅
```
Home 页面：
  - 设置渲染模式 = COMPACT
  - 设置缩放 = 1.0
  - 设置偏移 = (0, 0)
                ↓
        【单例实例】
                ↓
Index 页面：
  - 获取渲染模式 = COMPACT ← 已经是正确的！
  - 获取缩放 = 1.0 ← 已经是正确的！
  - 获取偏移 = (0, 0) ← 已经是正确的！
```

### 2. 消除初始化延迟 ✅
- **Before（多实例）**：Index 页面需要重新检测设备类型 → 设置渲染模式 → 可能有延迟
- **After（单例）**：Index 页面直接使用已有状态 → 无延迟

### 3. 消除闪烁和动画问题 ✅
- **Before（延迟显示）**：需要 30ms 延迟来掩盖模式切换
- **After（单例）**：模式已经正确，可以立即显示，无延迟感

### 4. 代码更简洁 ✅
- **Before**：大量防御性检查、延迟显示、状态同步代码
- **After**：简单的 getInstance() 调用，逻辑清晰

### 5. 性能更优 ✅
- **节省内存**：只创建一个 CanvasController 实例
- **减少初始化**：Index 页面不需要重新初始化
- **减少渲染**：不需要额外的延迟和动画处理

## 工作流程对比

### Before（多实例 + 延迟显示）
```
用户点击编辑按钮
    ↓
Home 页面：COMPACT 模式，小圆圈
    ↓
跳转到 Index 页面
    ↓
Index 创建新的 CanvasController 实例
    ↓
检测设备类型... (异步)
    ↓
设置渲染模式... (可能延迟)
    ↓
Canvas.onReady 触发
    ↓
渲染（透明状态，opacity=0）
    ↓
等待 30ms... ← 延迟掩盖问题
    ↓
显示 Canvas (opacity=1)
    ↓
用户看到：30ms 的轻微延迟 + 可能的闪烁
```

### After（单例模式）
```
用户点击编辑按钮
    ↓
Home 页面：COMPACT 模式，小圆圈，单例实例
    ↓
跳转到 Index 页面
    ↓
Index 获取单例实例（已经是 COMPACT 模式）✅
    ↓
Canvas.onReady 触发
    ↓
渲染 + 立即显示 (opacity=1) ← 无延迟
    ↓
用户看到：瞬间显示，完全无闪烁，无延迟感 🎉
```

## 日志输出示例

### 首次启动（Home 页面）
```
[CanvasController] 🎨 Single instance created
[CanvasController] ✅ First getInstance call - instance created
[Home] 🎨 Render mode set: COMPACT (小圆圈)
```

### 跳转到编辑页（Index 页面）
```
[CanvasController] ♻️ getInstance - reusing existing instance
[Index] ✅ Render mode already correct: COMPACT (from singleton)
[Index] 📺 Canvas onReady - renderMode: COMPACT (singleton)
[Index] 🎨 Rendering with singleton state - mode: COMPACT
[Index] ✅ Canvas visible (no delay needed with singleton)
```

## 兼容性和扩展性

### 1. 向后兼容 ✅
- 不影响现有功能
- 只改变实例创建方式
- API 接口保持不变

### 2. 易于扩展 ✅
- 可以轻松添加状态持久化
- 可以统一管理所有画布实例
- 便于全局状态监控和调试

### 3. 测试友好 ✅
- 可以添加 `resetInstance()` 方法用于单元测试
- 便于模拟和 Mock

## 潜在风险和注意事项

### 1. 回调函数管理 ⚠️
**问题**：Home 和 Index 可能设置不同的回调函数

**解决方案**：
- 每个页面在 `aboutToAppear` 中调用 `setCallbacks()` 设置自己的回调
- 单例只管理状态，不管理业务逻辑

```typescript
// Home.ets
aboutToAppear() {
  this.canvasController.setCallbacks(
    (task) => this.handleTaskSelect(task),
    // ... 其他回调
  );
}

// Index.ets
aboutToAppear() {
  this.canvasController.setCallbacks(
    (task) => this.projectManager.getActions().selectTask(task),
    // ... 其他回调
  );
}
```

### 2. 状态隔离 ⚠️
**问题**：某些状态可能需要页面独立

**解决方案**：
- 共享状态：渲染模式、缩放、偏移（这些应该保持一致）
- 独立状态：isDraggingCanvas、isDraggingNode（页面切换时自动重置）

### 3. 内存释放 ⚠️
**问题**：单例一直存在，会占用内存

**解决方案**：
- CanvasController 本身很轻量，内存占用可忽略
- 如果需要释放，可以添加 `destroy()` 方法

## 测试建议

### 测试场景
1. ✅ **Home → Index 切换**
   - 验证：渲染模式保持 COMPACT
   - 验证：无闪烁，无延迟

2. ✅ **Index → Home 返回**
   - 验证：状态同步
   - 验证：画布正常显示

3. ✅ **多次切换**
   - 验证：状态始终一致
   - 验证：无内存泄漏

4. ✅ **平板端测试**
   - 验证：NORMAL 模式正常工作
   - 验证：不受单例影响

### 验证方法
查看控制台日志，确认：
```
[CanvasController] 🎨 Single instance created  ← 只出现一次
[CanvasController] ♻️ getInstance - reusing existing instance  ← 后续都是复用
[Index] ✅ Render mode already correct: COMPACT (from singleton)  ← 状态已正确
[Index] ✅ Canvas visible (no delay needed with singleton)  ← 无延迟显示
```

## 总结

### 优化前的方案
- ✅ 提前设置渲染模式
- ✅ 延迟显示 Canvas（30ms）
- ❌ 治标不治本，仍有轻微延迟
- ❌ 代码复杂，防御性检查多

### 优化后的方案（单例模式）
- ✅ 状态完全同步
- ✅ 无需延迟显示
- ✅ 代码更简洁
- ✅ 性能更优
- ✅ 从根本上解决问题

### 关键优势
1. **根本性解决**：从源头消除多实例导致的状态不一致
2. **用户体验最佳**：无闪烁、无延迟、瞬间响应
3. **开发体验更好**：代码简洁、逻辑清晰、易于维护
4. **性能更优**：节省内存、减少初始化、减少不必要的渲染

### 适用场景
**非常适合**：
- 多页面共享画布状态
- 需要状态完全同步
- 追求极致用户体验

**不适合**：
- 需要完全隔离的多个画布实例
- 状态需要独立管理

对于 EasyFlow 应用，**单例模式是最优解**！🎉

