# 项目架构优化说明

## 优化概述

本次优化主要针对项目耦合性过高的问题，采用了分层架构和事件驱动模式，将原本紧密耦合的组件解耦，提高了代码的可维护性和可扩展性。

## 优化前的问题

1. **Index.ets 文件过于庞大** - 包含了UI渲染、业务逻辑、数据处理等多种职责
2. **组件间直接依赖** - 通过props传递大量回调函数，组件间耦合严重
3. **状态管理分散** - 状态分散在各个组件中，难以统一管理
4. **画布逻辑混乱** - 画布渲染和交互逻辑混合在一起

## 优化后的架构

### 1. 核心层 (Core Layer)

#### ProjectManager (项目管理器)
- **职责**: 统一管理项目状态和业务逻辑
- **功能**: 
  - 项目数据的增删改查
  - 任务管理
  - 关键路径计算
  - 状态管理
- **特点**: 单例模式，全局状态管理

#### CanvasRenderer (画布渲染器)
- **职责**: 专门处理图形渲染逻辑
- **功能**:
  - 任务节点渲染
  - 依赖连线渲染
  - 进度条绘制
- **特点**: 纯渲染逻辑，无状态

#### CanvasController (画布控制器)
- **职责**: 处理用户交互和画布操作
- **功能**:
  - 触摸事件处理
  - 拖拽逻辑
  - 连接模式管理
- **特点**: 状态管理，事件分发

#### EventBus (事件总线)
- **职责**: 组件间解耦通信
- **功能**:
  - 事件订阅/发布
  - 组件间消息传递
- **特点**: 单例模式，全局事件管理

### 2. 服务层 (Service Layer)

保持原有的服务层结构，但通过ProjectManager统一调用：
- StorageService - 数据存储
- CriticalPathService - 关键路径计算
- TemplateService - 模板管理
- 其他服务...

### 3. 组件层 (Component Layer)

#### ProjectSidebar (项目侧边栏)
- **职责**: 项目信息展示和操作
- **特点**: 直接使用ProjectManager，减少props传递

#### TaskEditor (任务编辑器)
- **职责**: 任务编辑界面
- **特点**: 通过事件总线与项目管理器通信

### 4. 页面层 (Page Layer)

#### Index.ets (主页面)
- **职责**: 页面布局和组件协调
- **特点**: 大幅简化，只负责组件组装和事件监听

## 架构优势

### 1. 职责分离
- **UI层**: 只负责界面展示
- **业务层**: 统一在ProjectManager中处理
- **渲染层**: 独立的画布渲染逻辑
- **交互层**: 独立的用户交互处理

### 2. 低耦合
- 组件间通过事件总线通信，不直接依赖
- 每个模块职责单一，易于测试和维护
- 新增功能时不影响现有代码

### 3. 高内聚
- 相关功能集中在同一模块中
- 状态管理统一，避免数据不一致
- 业务逻辑集中，便于理解和修改

### 4. 可扩展性
- 新功能可以通过事件总线轻松集成
- 组件可以独立开发和测试
- 架构支持插件化扩展

## 使用示例

### 1. 项目管理器使用
```typescript
const projectManager = ProjectManager.getInstance();
const actions = projectManager.getActions();

// 添加任务
actions.addTask();

// 更新任务
actions.updateTask(task);

// 计算关键路径
actions.calculateCriticalPath();
```

### 2. 事件总线使用
```typescript
const eventBus = EventBus.getInstance();

// 订阅事件
eventBus.on(Events.TASK_SELECTED, (task) => {
  console.log('任务被选中:', task);
});

// 发布事件
eventBus.emit(Events.TASK_UPDATED, updatedTask);
```

### 3. 画布控制器使用
```typescript
const canvasController = new CanvasController();
const actions = canvasController.getActions();

// 处理触摸事件
actions.handleTouch(event, project, selectedTaskId);

// 重置画布
actions.resetCanvas();
```

## 迁移指南

### 1. 现有组件迁移
- 将业务逻辑从组件中提取到ProjectManager
- 使用事件总线替代直接的回调函数传递
- 组件只保留UI相关的状态

### 2. 新功能开发
- 业务逻辑放在ProjectManager中
- UI组件通过事件总线与业务逻辑通信
- 渲染逻辑放在CanvasRenderer中

### 3. 测试策略
- 每个模块可以独立测试
- 使用事件总线进行集成测试
- 模拟ProjectManager进行组件测试

## 总结

通过这次架构优化，项目实现了：
- **代码行数减少**: Index.ets从582行减少到约200行
- **耦合度降低**: 组件间通过事件总线通信，不再直接依赖
- **可维护性提升**: 职责分离，每个模块功能单一
- **可扩展性增强**: 新功能可以轻松集成，不影响现有代码

这种架构模式为项目的长期发展奠定了良好的基础，支持团队协作开发和功能快速迭代。

