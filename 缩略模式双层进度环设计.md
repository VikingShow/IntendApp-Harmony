# 缩略模式双层进度环设计

## 📊 功能说明

在缩略模式下，任务节点采用双层进度环设计，可以同时直观地显示任务的时间进度和完成进度。

## 🎨 视觉设计

### 节点结构（从外到内）

```
┌─────────────────────────────────────┐
│  1. 外层选中高亮圈（可选）          │
│  2. 节点主体圆圈（彩色背景）        │
│  3. 外圈进度环（完成度）- 白色      │
│  4. 内圈进度环（时间进度）- 动态色  │
│  5. 关键路径标记（小红点，可选）    │
│  6. 任务名称（右侧文字）            │
└─────────────────────────────────────┘
```

### 进度环详细参数

#### 外圈进度环（完成度）
- **位置**: 半径 = CIRCLE_RADIUS - 4 (20px)
- **颜色**: 纯白色 `#FFFFFF`
- **线宽**: 4px
- **含义**: 显示任务的实际完成百分比（基于清单项完成情况）

#### 内圈进度环（时间进度）
- **位置**: 半径 = CIRCLE_RADIUS - 10 (14px)
- **颜色**: 动态调整
  - **正常**: 半透明白色 `rgba(255, 255, 255, 0.6)`
  - **延期警告**: 黄色 `rgba(255, 214, 10, 0.9)` / `rgba(255, 204, 0, 0.9)`
- **线宽**: 3px
- **含义**: 显示任务的时间消耗百分比（基于开始/结束日期）

### 延期预警逻辑

```typescript
if (timeProgress > completionProgress + 10) {
  // 时间进度远超完成进度（超过10%）→ 黄色警告
  // 说明任务进度落后于时间消耗
} else {
  // 正常进度 → 半透明白色
}
```

## 🎯 使用场景

### 场景一：进度正常
- 外圈（白色）：50% 完成
- 内圈（半透明白色）：45% 时间消耗
- **解读**：任务进度正常，完成度略超时间进度

### 场景二：进度延期
- 外圈（白色）：30% 完成
- 内圈（黄色）：60% 时间消耗
- **解读**：时间已过去60%，但只完成30%，需要加速

### 场景三：进度超前
- 外圈（白色）：80% 完成
- 内圈（半透明白色）：50% 时间消耗
- **解读**：提前完成，进度良好

## 💡 视觉优势

1. **双重信息展示**: 一个节点同时显示时间和完成两个维度的进度
2. **进度对比直观**: 通过内外圈的差异，快速识别任务状态
3. **延期预警明显**: 黄色内圈立即吸引注意力
4. **不干扰主要信息**: 进度环不影响节点颜色和名称显示
5. **适配深浅模式**: 颜色在深色/浅色主题下都清晰可见

## 🔧 技术实现

### 绘制顺序
1. 绘制节点主体圆圈（彩色背景）
2. 绘制节点边框
3. 绘制外圈进度环（完成度）
4. 绘制内圈进度环（时间进度）
5. 绘制关键路径标记
6. 绘制任务名称

### 关键代码
```typescript
// 外圈进度环（完成度）
if (completionProgress > 0) {
  this.context.beginPath();
  const startAngle = -Math.PI / 2; // 从12点钟方向开始
  const endAngle = startAngle + (Math.PI * 2 * completionProgress / 100);
  this.context.arc(centerX, centerY, CIRCLE_RADIUS - 4, startAngle, endAngle);
  this.context.strokeStyle = '#FFFFFF';
  this.context.lineWidth = 4;
  this.context.stroke();
}

// 内圈进度环（时间进度）
if (timeProgress > 0) {
  this.context.beginPath();
  const startAngle = -Math.PI / 2;
  const endAngle = startAngle + (Math.PI * 2 * timeProgress / 100);
  this.context.arc(centerX, centerY, CIRCLE_RADIUS - 10, startAngle, endAngle);
  // 动态颜色
  if (timeProgress > completionProgress + 10) {
    this.context.strokeStyle = 'rgba(255, 214, 10, 0.9)'; // 警告色
  } else {
    this.context.strokeStyle = 'rgba(255, 255, 255, 0.6)'; // 正常色
  }
  this.context.lineWidth = 3;
  this.context.stroke();
}
```

## 📱 交互说明

- **点击节点**: 选中该任务（显示选中高亮圈）
- **拖拽节点**: 移动任务位置
- **点击空白**: 取消选中

注：已移除节点详情对话框，进度信息完全通过可视化展示。

## 🎨 颜色参考

| 元素 | 浅色模式 | 深色模式 |
|-----|---------|---------|
| 外圈进度（完成度） | `#FFFFFF` | `#FFFFFF` |
| 内圈进度（正常） | `rgba(255, 255, 255, 0.6)` | `rgba(255, 255, 255, 0.6)` |
| 内圈进度（警告） | `rgba(255, 204, 0, 0.9)` | `rgba(255, 214, 10, 0.9)` |
| 节点边框 | `#D1D1D6` | `#48484A` |

---

**版本**: 1.0  
**更新日期**: 2025-10-10  
**作者**: EasyFlow 开发团队

