# EasyFlow 颜色系统使用指南

本文档介绍 EasyFlow 项目中的语义化颜色命名系统。所有颜色都基于功能和用途命名，而非颜色本身。

## 📋 目录

- [主题色系](#主题色系)
- [表面色系](#表面色系)
- [背景色系](#背景色系)
- [覆盖层颜色](#覆盖层颜色)
- [阴影颜色](#阴影颜色)
- [悬停效果颜色](#悬停效果颜色)
- [边框颜色](#边框颜色)
- [文字颜色](#文字颜色)
- [状态颜色](#状态颜色)
- [业务颜色](#业务颜色)
- [装饰性颜色](#装饰性颜色)

---

## 🎨 主题色系

### 主色调 (Primary)
用于品牌色、主要操作按钮、重要强调元素。

```typescript
ThemeConfig.primaryColor        // #007AFF - 主色
ThemeConfig.primaryLight        // #E3F2FD - 浅色背景
ThemeConfig.primaryDark         // #0056CC - 深色变体
ThemeConfig.primaryGradient     // 渐变色数组
ThemeConfig.primaryUltraLight   // #F0F8FF - 超浅色背景
```

**使用场景：**
- 主要操作按钮背景色
- 导航栏选中状态
- 重要信息提示
- 品牌标识

### 辅助色 (Secondary)
用于次要操作、辅助信息展示。

```typescript
ThemeConfig.secondaryColor      // #8E8E93
ThemeConfig.secondaryLight      // #F2F2F7
ThemeConfig.secondaryDark       // #1C1C1E
```

---

## 🏗️ 表面色系 (Surface)

表面色系用于卡片、面板等组件的表面颜色，提供视觉层次。

```typescript
ThemeConfig.surface             // #FFFFFF - 纯白表面
ThemeConfig.surfaceSubtle       // #F9FAFB - 微妙灰
ThemeConfig.surfaceLight        // #F2F2F7 - 浅灰表面
ThemeConfig.surfaceNeutral      // #E5E5EA - 中性表面
ThemeConfig.surfaceMuted        // #D1D1D6 - 弱化表面
ThemeConfig.surfaceStrong       // #C7C7CC - 强调表面
ThemeConfig.surfaceEmphasized   // #8E8E93 - 突出表面
ThemeConfig.surfaceBold         // #636366 - 粗体表面
ThemeConfig.surfaceDark         // #48484A - 深色表面
ThemeConfig.surfaceDeep         // #3A3A3C - 更深表面
ThemeConfig.surfaceDeepest      // #1C1C1E - 最深表面
```

**使用场景：**
- `surface`: 卡片、对话框背景
- `surfaceSubtle`: 页面背景、列表背景
- `surfaceLight`: 输入框背景、次级区域
- `surfaceNeutral`: 边框、分割线
- `surfaceEmphasized`: 次要文字颜色
- `surfaceDark/Deep/Deepest`: 主要文字颜色、深色主题

**迁移对照：**
```
white → surface
gray50 → surfaceSubtle
gray100 → surfaceLight
gray200 → surfaceNeutral
gray300 → surfaceMuted
gray400 → surfaceStrong
gray500 → surfaceEmphasized
gray600 → surfaceBold
gray700 → surfaceDark
gray800 → surfaceDeep
gray900 → surfaceDeepest
```

---

## 📦 背景色系 (Background)

专门用于不同层级的背景色。

```typescript
ThemeConfig.backgroundPrimary    // #FFFFFF - 主背景
ThemeConfig.backgroundSecondary  // #F2F2F7 - 次级背景
ThemeConfig.backgroundTertiary   // #E5E5EA - 三级背景
ThemeConfig.backgroundElevated   // #FFFFFF - 悬浮背景
ThemeConfig.backgroundCard       // #F8FAFC - 卡片背景
ThemeConfig.backgroundPage       // #FAFBFC - 页面背景
ThemeConfig.backgroundInput      // #F5F5F7 - 输入框背景
```

---

## 🌫️ 覆盖层颜色 (Overlay)

用于半透明的白色遮罩和浮层效果。

```typescript
ThemeConfig.overlayHeavy        // rgba(255,255,255,0.9) - 90% 不透明
ThemeConfig.overlayStrong       // rgba(255,255,255,0.8) - 80%
ThemeConfig.overlayMedium       // rgba(255,255,255,0.7) - 70%
ThemeConfig.overlayRegular      // rgba(255,255,255,0.6) - 60%
ThemeConfig.overlayLight        // rgba(255,255,255,0.5) - 50%
ThemeConfig.overlaySubtle       // rgba(255,255,255,0.4) - 40%
ThemeConfig.overlayFaint        // rgba(255,255,255,0.3) - 30%
ThemeConfig.overlayVeryFaint    // rgba(255,255,255,0.25) - 25%
ThemeConfig.overlayMinimal      // rgba(255,255,255,0.2) - 20%
ThemeConfig.overlayBare         // rgba(255,255,255,0.15) - 15%
ThemeConfig.overlayBarely       // rgba(255,255,255,0.1) - 10%
```

**使用场景：**
- 毛玻璃效果背景
- 弹窗遮罩层
- 悬浮菜单背景
- 半透明卡片

**迁移对照：**
```
transparentWhite90 → overlayHeavy
transparentWhite80 → overlayStrong
transparentWhite70 → overlayMedium
transparentWhite60 → overlayRegular
transparentWhite50 → overlayLight
transparentWhite40 → overlaySubtle
transparentWhite30 → overlayFaint
transparentWhite25 → overlayVeryFaint
transparentWhite20 → overlayMinimal
transparentWhite15 → overlayBare
transparentWhite10 → overlayBarely
```

---

## 🌑 阴影颜色 (Shadow Color)

用于阴影效果和暗色遮罩的颜色值。

```typescript
ThemeConfig.shadowColorStrong   // rgba(0,0,0,0.5) - 50%
ThemeConfig.shadowColorMedium   // rgba(0,0,0,0.4) - 40%
ThemeConfig.shadowColorRegular  // rgba(0,0,0,0.3) - 30%
ThemeConfig.shadowColorLight    // rgba(0,0,0,0.25) - 25%
ThemeConfig.shadowColorSubtle   // rgba(0,0,0,0.2) - 20%
ThemeConfig.shadowColorFaint    // rgba(0,0,0,0.15) - 15%
ThemeConfig.shadowColorMinimal  // rgba(0,0,0,0.1) - 10%
ThemeConfig.shadowColorBare     // rgba(0,0,0,0.05) - 5%
```

**使用场景：**
- 投影阴影颜色
- 暗色遮罩
- 边框颜色（需要半透明黑色时）

**注意：** 这些是颜色值。如需阴影配置对象，请使用 `ThemeConfig.shadowSmall`、`shadowMedium`、`shadowLarge` 等 ShadowConfig 对象。

**迁移对照：**
```
transparentBlack50 → shadowColorStrong
transparentBlack40 → shadowColorMedium
transparentBlack30 → shadowColorRegular
transparentBlack25 → shadowColorLight
transparentBlack20 → shadowColorSubtle
transparentBlack15 → shadowColorFaint
transparentBlack10 → shadowColorMinimal
transparentBlack05 → shadowColorBare
```

---

## ✨ 悬停效果颜色 (Hover)

用于按钮、链接等交互元素的悬停状态。

### 主色调悬停
```typescript
ThemeConfig.hoverPrimary        // rgba(0,122,255,0.3)
ThemeConfig.hoverPrimaryLight   // rgba(0,122,255,0.25)
ThemeConfig.hoverPrimarySubtle  // rgba(0,122,255,0.2)
ThemeConfig.hoverPrimaryFaint   // rgba(0,122,255,0.15)
ThemeConfig.hoverPrimaryMinimal // rgba(0,122,255,0.1)
ThemeConfig.hoverPrimaryBare    // rgba(0,122,255,0.05)
```

### 功能色悬停
```typescript
ThemeConfig.hoverSuccess        // rgba(52,199,89,0.2)
ThemeConfig.hoverSuccessLight   // rgba(52,199,89,0.1)
ThemeConfig.hoverWarning        // rgba(255,149,0,0.2)
ThemeConfig.hoverWarningLight   // rgba(255,149,0,0.1)
ThemeConfig.hoverError          // rgba(255,59,48,0.2)
ThemeConfig.hoverErrorLight     // rgba(255,59,48,0.1)
```

### 中性色悬停
```typescript
ThemeConfig.hoverNeutral        // rgba(142,142,147,0.2)
ThemeConfig.hoverNeutralLight   // rgba(142,142,147,0.1)
ThemeConfig.hoverNeutralFaint   // rgba(142,142,147,0.05)
```

**使用场景：**
- 列表项悬停背景
- 按钮悬停效果
- 选中状态背景

**迁移对照：**
```
transparentPrimary30 → hoverPrimary
transparentPrimary25 → hoverPrimaryLight
transparentPrimary20 → hoverPrimarySubtle
transparentPrimary15 → hoverPrimaryFaint
transparentPrimary10 → hoverPrimaryMinimal
transparentPrimary05 → hoverPrimaryBare

transparentSuccess20 → hoverSuccess
transparentSuccess10 → hoverSuccessLight
transparentWarning20 → hoverWarning
transparentWarning10 → hoverWarningLight
transparentError20 → hoverError
transparentError10 → hoverErrorLight

transparentGray20 → hoverNeutral
transparentGray10 → hoverNeutralLight
transparentGray05 → hoverNeutralFaint
```

---

## 🔲 边框颜色 (Border)

```typescript
ThemeConfig.borderDefault       // #E2E8F0 - 默认边框
ThemeConfig.borderGrid          // #E0E0E0 - 网格线
ThemeConfig.borderGridFine      // #F0F0F0 - 细网格线
ThemeConfig.borderDivider       // #E2E8F0 - 分割线
```

**迁移对照：**
```
borderLight → borderDefault
borderBottom → borderDivider
```

---

## 📝 文字颜色 (Text)

```typescript
ThemeConfig.textPrimary         // #1D1D1F - 主要文字
ThemeConfig.textSecondary       // #666666 - 次要文字
ThemeConfig.textTertiary        // #86868B - 三级文字
ThemeConfig.textMuted           // #64748B - 弱化文字
ThemeConfig.textSubdued         // #94A3B8 - 低调文字
ThemeConfig.textStrong          // #1E293B - 强调文字
ThemeConfig.textSuccess         // #28A745 - 成功文字
```

**使用场景：**
- `textPrimary`: 标题、重要内容
- `textSecondary`: 正文、常规内容
- `textTertiary`: 辅助说明、提示信息
- `textMuted`: 次要提示
- `textSubdued`: 禁用状态文字
- `textStrong`: 特别强调的文字

**迁移对照：**
```
textDescription → textTertiary
textLight → textSubdued
textDark → textStrong
```

---

## 🎯 状态颜色 (Status)

### 成功状态
```typescript
ThemeConfig.successColor        // #34C759
ThemeConfig.successLight        // #E8F5E8
```

### 警告状态
```typescript
ThemeConfig.warningColor        // #FF9500
ThemeConfig.warningLight        // #FFF4E6
```

### 错误状态
```typescript
ThemeConfig.errorColor          // #FF3B30
ThemeConfig.errorLight          // #FFEBEE
```

### 信息状态
```typescript
ThemeConfig.infoColor           // #5AC8FA
ThemeConfig.infoLight           // #E3F2FD
```

---

## 💼 业务颜色 (Business)

### 任务状态
```typescript
ThemeConfig.taskCritical        // #D0021B - 关键任务
ThemeConfig.taskNormal          // #4A90E2 - 普通任务
ThemeConfig.taskSelected        // #FF8C00 - 选中任务
ThemeConfig.taskProgress        // #2ECC71 - 进行中任务
```

### 项目类型
```typescript
ThemeConfig.projectDevelopment  // #3B82F6 - 开发项目
ThemeConfig.projectDesign       // #8B5CF6 - 设计项目
ThemeConfig.projectMarketing    // #F59E0B - 营销项目
ThemeConfig.projectResearch     // #10B981 - 研究项目
ThemeConfig.projectOther        // #6B7280 - 其他项目
```

### 画布网格
```typescript
ThemeConfig.canvasGridLine      // #E0E0E0 - 网格主线
ThemeConfig.canvasGridLineFine  // #F0F0F0 - 网格细线
```

**迁移对照：**
```
gridLine → canvasGridLine
gridLineFine → canvasGridLineFine
```

---

## 🎨 装饰性颜色 (Decorative)

用于背景装饰元素和文化词汇展示。

```typescript
ThemeConfig.decorativeSlate     // #E2E8F0
ThemeConfig.decorativeSlateDark // #CBD5E1
ThemeConfig.decorativeStone     // #D1D5DB
ThemeConfig.decorativePearl     // #E5E7EB
ThemeConfig.decorativeFog       // #F3F4F6
ThemeConfig.decorativeCloud     // #F1F5F9
ThemeConfig.decorativeMist      // #F8FAFC
ThemeConfig.decorativeFrost     // #F9FAFB
ThemeConfig.decorativeSnow      // #FFFAFBFC
ThemeConfig.decorativeIce       // #FCFCFD
```

**迁移对照：**
```
culturalWord1 → decorativeSlate
culturalWord2 → decorativeSlateDark
culturalWord3 → decorativeStone
culturalWord4 → decorativePearl
culturalWord5 → decorativeFog
culturalWord6 → decorativeCloud
culturalWord7 → decorativeMist
culturalWord8 → decorativeFrost
culturalWord9 → decorativeSnow
culturalWord10 → decorativeIce
```

---

## 🔄 特殊颜色

### 透明
```typescript
ThemeConfig.transparent         // #00000000 - 完全透明
```

**迁移对照：**
```
transparentNone → transparent
```

---

## 📚 最佳实践

### ✅ 推荐做法

1. **根据用途选择颜色**
   ```typescript
   // ✅ 好 - 语义明确
   .backgroundColor(ThemeConfig.surface)
   .fontColor(ThemeConfig.textPrimary)
   
   // ❌ 避免 - 基于颜色名称
   // .backgroundColor(ThemeConfig.white)
   // .fontColor(ThemeConfig.gray800)
   ```

2. **保持一致性**
   ```typescript
   // ✅ 在整个应用中统一使用相同的颜色用于相同的目的
   .backgroundColor(ThemeConfig.overlayMedium)  // 毛玻璃效果
   .backdropBlur(ThemeConfig.backdropBlurMedium)
   ```

3. **遵循视觉层次**
   ```typescript
   // 主标题
   .fontColor(ThemeConfig.textPrimary)
   
   // 副标题
   .fontColor(ThemeConfig.textSecondary)
   
   // 辅助说明
   .fontColor(ThemeConfig.textTertiary)
   ```

### ⚠️ 注意事项

1. **避免直接使用颜色值**
   - 总是使用 `ThemeConfig` 中定义的颜色
   - 不要在代码中硬编码颜色值

2. **选择合适的透明度**
   - 覆盖层使用 `overlay` 系列
   - 阴影使用 `shadow` 系列
   - 悬停效果使用 `hover` 系列

3. **考虑对比度**
   - 确保文字与背景有足够的对比度
   - 重要操作使用高对比度颜色

---

## 🔧 迁移指南

如果你正在更新旧代码，请参考以下对照表：

| 旧命名 | 新命名 | 用途 |
|--------|--------|------|
| `white` | `surface` | 纯白表面 |
| `gray50` | `surfaceSubtle` | 最浅灰 |
| `gray100` | `surfaceLight` | 浅灰 |
| `gray200` | `surfaceNeutral` | 中性灰（常用于边框） |
| `gray500` | `surfaceEmphasized` | 中灰（常用于次要文字） |
| `gray800` | `surfaceDeep` | 深灰（常用于主要文字） |
| `transparentWhite*` | `overlay*` | 白色覆盖层 |
| `transparentBlack*` | `shadowColor*` | 黑色阴影颜色 |
| `transparentPrimary*` | `hoverPrimary*` | 主色悬停效果 |
| `transparentNone` | `transparent` | 完全透明 |
| `culturalWord*` | `decorative*` | 装饰性颜色 |
| `borderLight` | `borderDefault` | 默认边框 |
| `borderBottom` | `borderDivider` | 分割线 |
| `textDescription` | `textTertiary` | 三级文字 |
| `gridLine` | `canvasGridLine` | 画布网格线 |
| `gridLineFine` | `canvasGridLineFine` | 画布细网格线 |

---

## 📖 相关文档

- [ThemeConfig.ets](entry/src/main/ets/utils/ThemeConfig.ets) - 完整的颜色定义
- [color.json](entry/src/main/resources/base/element/color.json) - 颜色资源配置

---

*最后更新：2024年*

